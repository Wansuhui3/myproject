# 雷达波动分析与真值对比系统：技术架构与算法说明

> 用途：作为当前代码的功能、算法和重构依据说明。代码基线为当前 `radar_wave_analyzer` 工作区。

## 1. 系统定位

项目是基于 Dash、Flask、Pandas、NumPy、Plotly、SciPy、OpenPyXL 和 pywebview 的本地桌面化雷达分析工具。启动后由 pywebview 承载本机 Dash 页面。

系统有两条主业务线：

1. **波动分析**：加载雷达数据，完成目标轨迹分段、物理量曲线、帧间波动统计、框选统计和摘要快照。
2. **真值对比**：加载雷达与 RTK 数据，完成目标关联、延迟/坐标诊断、对齐、误差分析、性能验收和 Excel 导出。

总体链路为：`上传 CSV/JSON → 解析和清洗 → 领域算法 → 缓存/状态 → 图表、统计面板、导出文件`。

## 2. 运行与入口

| 文件 | 职责 |
|---|---|
| `launcher.py` | 选择本地端口、后台启动 Dash、显示 pywebview 加载页和桌面窗口。 |
| `radar_wave_analyzer/app.py` | 应用装配；设置布局并导入回调。 |
| `radar_wave_analyzer/extensions.py` | 创建 Dash、Flask-Caching 和 Flask session。 |
| `radar_wave_analyzer/components/layout.py` | 定义页面组件、Store 与左右中栏布局。 |
| `radar_wave_analyzer/callbacks.py` | 接收 UI 事件，编排数据、算法、图表、面板和下载。 |

运行流程：`launcher.py → 本地 Flask/Dash 服务 → pywebview 轮询服务 → Dash 页面加载`。服务只监听 `127.0.0.1`，不提供局域网访问。

## 3. 目录与模块职责

### 3.1 基础设施

| 模块 | 核心职责 |
|---|---|
| `config.py` / `config.yaml` | 默认值、YAML 深度合并、参数校验。 |
| `cache.py` | 按 Flask session 隔离主页面数据、对齐数据和性能结果。 |
| `core/export_location.py` | 导出目录定位和创建。 |
| `check_deps.py` | 环境依赖检查。 |

### 3.2 波动分析

| 模块 | 核心职责 |
|---|---|
| `core/data_loader.py` | CSV 解码、时间解析、清洗、来源识别。 |
| `core/json_loader.py` | JSON topic/object/字段数组展开与规范化。 |
| `core/segmenter.py` | 轨迹生命周期分段。 |
| `core/wave_calc.py` | 帧间差分、波动统计和最大跳变定位。 |
| `core/selection.py` | Plotly 框选范围提取。 |
| `components/graph_builder.py` | 多物理量子图、展示降采样、跳变和框选高亮。 |
| `components/stats_panel.py` | 整段/框选统计和波动摘要渲染。 |

### 3.3 真值对比

| 模块 | 核心职责 |
|---|---|
| `comparison/parser.py` | CSV/JSON 角色识别、时间统一、字段别名、清洗。 |
| `comparison/matching.py` | 生命周期段、MotionStatus 过滤、运动判断、ID 关联。 |
| `comparison/id_finder.py` | 旧版兼容 KDTree 目标发现。 |
| `comparison/coord_diag.py` | 坐标偏差诊断。 |
| `comparison/delay_detect.py` | 延迟扫描。 |
| `comparison/alignment.py` | 对齐、插值、门控、误差与自定义映射。 |
| `comparison/service.py` | 无 UI 的上传、选择、诊断、对齐编排。 |
| `comparison/performance.py` | 性能验收计算。 |
| `comparison/exporter.py` | xlsx/CSV 导出及历史导出函数。 |
| `components/comparison_charts.py` | 对比图、中断、单线/双线展示。 |
| `components/performance_panel.py` | 验收表、性能摘要和快照摘要。 |

## 4. 配置体系

配置加载为：`_DEFAULTS + config.yaml → 深度合并 → _validate_config() → get(key)`。

| 配置域 | 典型参数 |
|---|---|
| 采样与分段 | `SAMPLING_PERIOD_MS`、`GAP_THRESHOLD`、`MIN_TRAJ_FRAMES`、Track_Age 回绕阈值。 |
| 空间规则 | `SPATIAL_SPLIT_ENABLED`、`MAX_TRACK_SPEED`、`POS_JUMP_THRESHOLD`。 |
| 图表 | `DISPLAY_MAX_POINTS`、`GRAPH_RENDERER`、`RESAMPLER_ENABLED`。 |
| 真值对比 | 空间/时间门限、延迟扫描、ID 关联、坐标偏差门限。 |
| 性能验收 | 距离段、准确率、连续三帧要求、物理量动态限值。 |

优点是算法阈值大多未硬编码；不足是算法、显示与导出配置共用同一层级，建议后续拆分为领域配置。

## 5. 关键数据结构

### 5.1 主页面雷达 DataFrame

典型字段包括 `timestamp`、`timestamp_parsed`、`ID`、`Track_Age`、`Dx/Dy/Vx/Vy`、`file_index`、`source_filename`、`radar_source_key`、`radar_source_group`。

### 5.2 分段结果

`segment_trajectories()` 输出 `meta_df` 和 `segments`。`meta_df` 一行表示一个轨迹段，含原始 ID、起止时间、帧数、Track_Age、来源和异常标记。缓存中分段优先压缩为主表 `int64` 行号数组，通过 `df.iloc[...]` 恢复，以避免大量 DataFrame 副本。

### 5.3 真值对比上传结果

每个文件/JSON topic 统一表示为字典，包含角色、DataFrame、文件名、时间范围、采样率、物理量字段、原始字段到内部字段映射、错误和告警。

### 5.4 对齐结果 `aligned_df`

核心列包含雷达位置速度、RTK 插值位置速度、位置/速度误差、匹配距离、雷达/RTK 帧号、时间差、空间门控、时间门控、RTK 范围和最终 `is_matched`。自定义映射生成 `radar[字段]`、`rtk[字段]`、`error[字段-字段]`。

### 5.5 性能结果

每个物理量包含 `available`、`mode`、逐帧 `frames`、距离段 `bins`、整体 `overall` 和覆盖率 `coverage`。

## 6. 波动分析算法

### 6.1 加载和分段

CSV 支持 UTF-8、GBK、UTF-8-SIG；主页面至少要求 `timestamp`、`ID`、`Track_Age`。JSON 支持 `topic → target ID → field arrays`，一个 JSON 的多个 topic 会展开为多个来源。

轨迹先按来源组、文件和 ID 分组，再按规则分段：

| 规则 | 条件 | 行为 |
|---|---|---|
| A | ID 首次出现 | 开始新段。 |
| B | Track_Age uint8 正常回绕 | 不切段。 |
| C | 非回绕式 Track_Age 下降 | 切新段。 |
| D | 相邻时间差超过 `GAP_THRESHOLD` | 切新段。 |
| E | `file_index` 变化 | 强制切新段。 |
| F | 空间速度/距离异常 | 仅空间切分开关开启时切段。 |
| G | 生命周期字段结束→开始 | 配置后切段。 |

回绕展开公式为：\(age'_i=age_i+256\times N_{wrap}\)。

### 6.2 帧间波动

对物理量序列：\(\Delta x_i=x_i-x_{i-1}\)。首帧为 `NaN`。当相邻时间间隔大于 `2 × SAMPLING_PERIOD_MS` 时，差分也置为 `NaN`，避免将丢帧后的长期变化误计为单帧跳变。

统计包括平均绝对波动、样本标准差、RMS、峰峰值、最大正跳变和最大负跳变。`find_max_jump()` 定位 \(\arg\max_i|\Delta x_i|\)，用于曲线红线标记。

### 6.3 图表与框选

- 每个物理量一个纵向子图，共享时间轴。
- 不超过 `DISPLAY_MAX_POINTS` 时完整显示；超过时仅展示层降采样，统计仍基于全量。
- 最大跳变使用 Plotly shape，减少额外 trace。
- 框选优先用 Plotly `selectedData.range`，使统计范围与鼠标框边界一致。

## 7. 真值文件解析和目标关联

### 7.1 角色与时间

角色判定为：`center_x + center_y → RTK`，否则 `Dx + Dy → 雷达`，否则 unknown。RTK 优先避免误判。

CSV 下划线时间按本地墙钟转换为 epoch 秒；JSON 数值时间按量级识别秒、毫秒、微秒或纳秒，再统一为 epoch 秒。解析器保留原始字段，并建立如 `CX→center_x`、`DX→Dx` 的内部别名。

### 7.2 生命周期与 MotionStatus

真值对比有独立的候选生命周期提取规则：Track_Age 非回绕重置还需连续确认和明显位置跳变，才认定为新物理目标。

| MotionStatus | 行为 |
|---|---|
| 1（运动） | 保留。 |
| 2（刹停） | 同一段出现过状态 1 时保留，否则过滤。 |
| 3（静止） | 过滤。 |
| 4（横穿） | 保留。 |

候选还需满足 P90 速度、P90 加速度或最大位移中的至少一项运动门限。

### 7.3 雷达/RTK ID 关联

对每个雷达候选段和 RTK ID，在真实时间重叠范围内插值 RTK 坐标，计算 \(d_i=\sqrt{(Dx_i-cx_i)^2+(Dy_i-cy_i)^2}\)。满足 \(d_i<match\_threshold\) 的帧计为命中，再计算覆盖率和门控后 RMSE。

候选成本为 \(cost=RMSE+(1-coverage)\times match\_threshold\)，按升序分配。RTK 占用键是 `(radar_source_key, rtk_id, rtk_file_index)`：同一来源、时间重叠候选不能重复占用；FLR 与 RLR 可各自关联同一 RTK。

## 8. 坐标诊断、延迟与对齐

### 8.1 坐标和延迟诊断

`coord_diag.py` 输出坐标偏差提示，不自动修正数据。延迟扫描对候选 \(d\) 使用 \(t'_{radar}=t_{radar}+d/1000\)，在每一个候选延迟下插值 RTK、空间门控并计算 RMSE、命中帧数和匹配率。

只有满足最小命中帧数和最小匹配率的延迟候选才可参与最优选择。由于不同候选可能使用不同门控样本集，解释延迟结果必须同时查看 RMSE、匹配帧数和匹配率。

### 8.2 对齐公式

对齐顺序：时间补偿 → RTK 线性插值 → 时间门控 → 空间门控 → 误差计算。

位置距离：\(d_i=\sqrt{(Dx_i-cx_i)^2+(Dy_i-cy_i)^2}\)。

空间有效：\(d_i<T_{space}\)。

时间有效：\(|t'_i-t_{nearestRTK}|\le T_{time}\)，且时刻在 RTK 时间范围内。

最终匹配：\(is\_matched_i=spatial\_matched_i\land time\_gated_i\)。

位置误差：\(e_x=Dx-cx\)、\(e_y=Dy-cy\)、\(e_{pos}=\sqrt{e_x^2+e_y^2}\)。

主 RMSE 只统计 `is_matched=True` 帧；同时输出空间拒绝帧、时间拒绝帧、RTK 范围外帧和时间有效帧，避免仅看 RMSE 造成误判。

### 8.3 自定义映射

| 映射状态 | 行为 |
|---|---|
| 两侧都有字段 | 双线显示；单位兼容时可算误差。 |
| 只选雷达 | 雷达单线，不算误差。 |
| 只选真值 | 真值单线，不算误差。 |
| 两侧均空 | 忽略该通道。 |

## 9. 对比图中断逻辑

中断检测以完整雷达时间序列为准：取相邻正时间间隔的低分位数作为标称周期，当 `gap > nominal_period × radar_gap_break_factor` 时判为真实中断。曲线在该处插入 `None` 并绘制红色叉号。

RTK 曲线使用完整真值时间域，不会因雷达缺帧同步消失。x 轴采用 epoch 毫秒 date 轴，悬浮框按本地时区显示，二者口径一致。

## 10. 性能验收算法

有效样本必须满足：已匹配、雷达值有限、真值有限、RTK 纵向距离有限且在有效距离范围内。距离分桶永远使用 \(d=|rtk\_center\_x|\)，不使用雷达 Dx。

单帧误差：\(s_i=x_i-q_i\)，绝对误差：\(e_i=|s_i|\)，单帧通过：\(e_i<T_i\)，准确率：\(accuracy=pass\_count/valid\_count\)。当前准确率要求是严格比较，例如 `accuracy > 95.45%`。

支持四类动态限值：`absolute`、`percent`、`max_absolute_percent`、`scaled_max_absolute_percent`。

连续三帧先归一化：\(r_i=e_i/L_i\)，再计算：\(R3=max(min(r_i,r_{i+1},r_{i+2}))\)。只有同一三帧窗口三帧均达到或超过对应三帧限值，才认定为严重连续违规。未匹配、无效样本和真实时间中断都会打断连续性。

每段结论为通过、不通过、样本不足或不可判定。任一可判定距离段失败则总体失败；任一有数据段样本不足则总体样本不足。`Ay` 的 `single_limit` 模式不分距离段，要求完整曲线所有有效帧满足单一误差限值。

## 11. 快照、导出和缓存

性能摘要与波动摘要仅在点击“插入当前”时写入浏览器 Store；重新分析不会覆盖快照，但刷新页面或重启程序会丢失。

生产导出路径是：`aligned_df + 映射通道 + performance_results → export_comparison_workbook_bytes() → xlsx 下载`。工作簿包括验收汇总、各物理量逐帧明细、动态阈值、Excel 公式、三帧判定、红色失败标记、筛选和冻结窗格。

缓存键按 session 隔离，覆盖主页面 `df/meta_df/segments` 与对比页面 `radar_df/rtk_df/aligned_df/summary/rtk_curve_df/perf_results`。当前采用 `SimpleCache`，不支持跨进程和持久化。

## 12. 依赖和耦合

```text
config.py → core/*、comparison/matching.py、comparison/service.py、performance.py
extensions.py → app.py、cache.py
cache.py → callbacks.py
callbacks.py → core/*、comparison/*、components/*、cache.py
comparison/service.py → parser.py、matching.py、alignment.py、delay_detect.py、coord_diag.py
```

主要耦合风险：

| 耦合点 | 风险 | 重构方向 |
|---|---|---|
| `callbacks.py` | 约三千行，UI、状态、渲染和业务编排混合。 | 拆为波动回调、对比回调、ViewModel、组件工厂。 |
| DataFrame 列名 | 多处直接依赖 `Dx/Dy/Vx/Vy/center_x`。 | 引入 schema/adapter 层。 |
| 映射字典 | 原始列、内部列、输出列混用。 | 引入 `FieldMapping` 数据模型。 |
| 连续性规则 | 分段、图表中断、三帧规则分散。 | 引入统一 `ContinuityPolicy`。 |
| 缓存键 | 回调直接感知缓存结构。 | 引入分析会话服务或 Repository。 |
| 配置 | 算法、UI、导出共用 YAML。 | 拆分领域配置和版本校验。 |

## 13. 推荐重构路线

### 第一阶段：低风险整理

1. 拆分 `callbacks.py`。
2. 删除已确认无入口的图片/JSON 导出和旧测试。
3. 清理调试脚本、旧日志和无效打包产物。
4. 统一格式化、空态组件和状态文本。

### 第二阶段：领域模型

建议引入 `RadarTrack`、`TrackSegment`、`GroundTruthTrack`、`TrackAssociation`、`AlignmentRequest`、`AlignmentResult`、`FieldMapping`、`MetricRule`、`MetricResult`、`PerformanceSnapshot`。DataFrame 保留为计算载体，但不再作为跨层唯一接口。

### 第三阶段：统一策略

建立统一连续性策略，例如 `ContinuityPolicy(nominal_period_strategy="p25", gap_factor=2.5, break_on_unmatched=True)`，明确区分轨迹生命周期切分、图表断线、验收连续性和文件边界。

### 第四阶段：持久化与性能

单机可使用 SQLite + Parquet；多用户可使用 Redis + 对象存储；大文件可转 Parquet 后按目标段读取；快照可选持久化；大型导出可异步化。

### 第五阶段：测试升级

补充上传到页面结果的端到端测试、FLR/RLR 同传关联回归、延迟样本集稳定性测试、大文件性能基准、页面状态机测试和脱敏真实数据 golden regression 测试。

## 14. 结论

当前算法层已经较清晰地拆分为分段、关联、对齐、性能验收和导出；主要技术债务集中于 UI 编排、自由格式映射、直接列名依赖和内存缓存生命周期。

推荐升级顺序：**先拆 UI 编排与状态管理，再引入领域模型与统一连续性策略，最后升级缓存、持久化和异步任务基础设施。**
