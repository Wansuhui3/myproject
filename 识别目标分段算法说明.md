# 识别目标分段算法说明

> 模块路径：`radar_wave_analyzer/comparison/alignment.py`
> 核心函数：`compute_distance_bin_stats(aligned_df, bins)`
> 文档版本：2026-07-21

---

## 1. 算法概述

本算法用于**雷达目标轨迹与 RTK 真值对比**场景下，对"识别到的单个目标"按**纵向距离**进行分段（分桶）统计。其上游是 `align_trajectories` 产出的逐帧对齐结果 `aligned_df`，本算法在其基础上：

1. 仅保留通过空间匹配与时间门控的**有效帧**（`is_matched == True`）；
2. 以雷达纵向距离 `radar_Dx` 作为分段指标，按配置的距离边界列表 `bins` 将帧划入若干左闭右开区间 `[lo, hi)`；
3. 对每个区间统计位置误差 `pos_error_abs` 的**帧数 / 均值 / 标准差 / RMSE / 最大值**。

该算法的目的是让开发者能够观察"不同距离段下雷达定位误差的变化规律"，是数据预览右侧"分距离区间统计"卡片的数据来源。

### 1.1 在系统中的位置

```
雷达CSV ┐
        ├─► align_trajectories() ──► aligned_df (逐帧对齐+误差)
RTK CSV ┘                                     │
                                              ▼
                                  compute_distance_bin_stats(aligned_df, bins)
                                              │
                                              ▼
                                   bin_stats: [{bin, frames, mean, std, rmse, max}, ...]
                                              │
                                              ▼
                          render_cmp_distance_bins() ──► 右侧面板 "分距离区间统计" 卡片
```

---

## 2. 输入输出定义

### 2.1 输入

| 参数 | 类型 | 说明 |
|------|------|------|
| `aligned_df` | `pd.DataFrame` | `align_trajectories` 返回的逐帧对齐结果。关键列见下表。 |
| `bins` | `list[float]` | 距离边界列表，如 `[0, 10, 20, 30, 40, 50, 80, 120]`。相邻两值构成一个区间 `[bins[i], bins[i+1])`。 |

`aligned_df` 中本算法使用到的列：

| 列名 | 含义 | 用途 |
|------|------|------|
| `radar_Dx` | 雷达测得的纵向距离（米） | **分桶指标** |
| `pos_error_abs` | 位置误差绝对值 = `sqrt(pos_error_x² + pos_error_y²)`（米） | 每桶统计对象 |
| `is_matched` | 是否为有效匹配帧（空间匹配 AND 时间门控通过） | 过滤无效帧 |

### 2.2 输出

返回 `list[dict]`，每个元素对应一个距离区间：

```python
[
    {'bin': '0-10m', 'frames': 120, 'mean': 0.152, 'std': 0.083, 'rmse': 0.173, 'max': 0.421},
    {'bin': '10-20m', 'frames': 98,  'mean': 0.201, 'std': 0.110, 'rmse': 0.229, 'max': 0.612},
    # ... 每个区间一个 dict
]
```

| 字段 | 类型 | 含义 |
|------|------|------|
| `bin` | `str` | 区间标签，格式 `"{lo}-{hi}m"` |
| `frames` | `int` | 该区间内有效帧数（0 帧时其余统计量为 `None`） |
| `mean` | `float \| None` | 位置误差绝对值的算术均值 |
| `std` | `float \| None` | 位置误差绝对值的样本标准差 |
| `rmse` | `float \| None` | 均方根误差 = `sqrt(mean(error²))` |
| `max` | `float \| None` | 位置误差绝对值的最大值 |

> 注意：`mean` / `std` / `rmse` / `max` 均基于**误差绝对值** `pos_error_abs` 计算，而非带符号误差，因此反映的是误差"幅度"而非方向。

---

## 3. 核心分段逻辑

### 3.1 处理步骤

1. **空数据短路**：若 `aligned_df` 为空，直接返回 `[]`。
2. **有效帧过滤**：若含 `is_matched` 列，仅保留 `is_matched == True` 的帧（与总体误差摘要保持同一统计口径，避免未匹配帧污染 RMSE/P95）。过滤后仍为空则返回 `[]`。
3. **遍历区间**：对 `bins` 中每对相邻边界 `(lo, hi)`：
   - 构造掩码 `mask = (radar_Dx >= lo) & (radar_Dx < hi)`（左闭右开）；
   - 取子帧 `seg = aligned_df[mask]`；
   - 若 `seg` 为空，输出全 `None` 的占位记录；
   - 否则对 `seg['pos_error_abs']` 去 NaN 后计算 `mean / std / rmse / max`。
4. **返回结果列表**。

### 3.2 核心代码

```python
def compute_distance_bin_stats(aligned_df: pd.DataFrame, bins: list) -> list:
    """按距离区间分桶统计位置误差。

    使用 radar_Dx (纵向距离) 作为距离指标进行分桶。
    对每个区间 [lo, hi) 统计 pos_error_abs 的帧数/Mean/Std/RMSE/Max。
    """
    if len(aligned_df) == 0:
        return []

    # 误差分桶与总体摘要保持同一口径：仅统计有效匹配帧。
    if 'is_matched' in aligned_df.columns:
        aligned_df = aligned_df[aligned_df['is_matched']]
    if len(aligned_df) == 0:
        return []

    results = []
    for i in range(len(bins) - 1):
        lo = bins[i]
        hi = bins[i + 1]
        mask = (aligned_df['radar_Dx'] >= lo) & (aligned_df['radar_Dx'] < hi)
        seg = aligned_df[mask]

        if len(seg) == 0:
            results.append({
                'bin': f'{lo}-{hi}m',
                'frames': 0, 'mean': None, 'std': None, 'rmse': None, 'max': None,
            })
            continue

        errors = seg['pos_error_abs'].values
        valid = errors[~np.isnan(errors)]
        results.append({
            'bin': f'{lo}-{hi}m',
            'frames': len(seg),
            'mean': round(float(np.mean(valid)), 3) if len(valid) > 0 else None,
            'std':  round(float(np.std(valid)), 3) if len(valid) > 0 else None,
            'rmse': round(float(np.sqrt(np.mean(valid ** 2))), 3) if len(valid) > 0 else None,
            'max':  round(float(np.max(np.abs(valid))), 3) if len(valid) > 0 else None,
        })

    return results
```

### 3.3 单区间统计量计算口径

每桶的 `mean / std / rmse / max` 由 `_compute_error_metrics` 统一口径计算（该函数同样服务于总体误差摘要）：

```python
def _compute_error_metrics(errors: np.ndarray) -> dict:
    valid = errors[~np.isnan(errors)]
    if len(valid) == 0:
        return {'mean': None, 'std': None, 'rmse': None, 'max': None, 'p50': None, 'p95': None}
    abs_valid = np.abs(valid)
    return {
        'mean': round(float(np.mean(valid)), 4),
        'std':  round(float(np.std(valid)), 4),
        'rmse': round(float(np.sqrt(np.mean(valid ** 2))), 4),
        'max':  round(float(np.max(abs_valid)), 4),
        'p50':  round(float(np.percentile(abs_valid, 50)), 4),
        'p95':  round(float(np.percentile(abs_valid, 95)), 4),
    }
```

> 分段算法当前仅取用其中的 `mean / std / rmse / max` 四个字段；`p50 / p95` 在总体摘要中使用，扩展时可按需引入。

---

## 4. 参数说明

### 4.1 `bins`（距离边界列表）

| 属性 | 说明 |
|------|------|
| 来源 | 配置文件 `config.yaml` 的 `comparison.distance_bins` |
| 默认值 | `[0, 10, 20, 30, 40, 50, 80, 120]` |
| 语义 | 相邻两值构成左闭右开区间 `[lo, hi)`；最后一个区间上限为 `bins[-1]` |
| 区间数 | `len(bins) - 1` |

**配置校验**（`config.py`）：`distance_bins` 必须满足：
- 类型为 `list` / `tuple`，且长度 ≥ 2；
- 每个元素为数值（非 `bool`）；
- 严格单调递增（`bins[i] < bins[i+1]` 对所有 `i` 成立）。

校验失败会在应用启动时收集到 `errors` 列表并提示，避免运行时出现空区间或反向区间。

### 4.2 `match_threshold_m` / `time_gate_ms`（上游对齐参数，间接影响分段样本）

分段算法的输入质量依赖于 `align_trajectories` 的匹配结果，相关参数：

| 参数 | 默认值 | 作用 |
|------|--------|------|
| `match_threshold_m` | `5.0` | 空间匹配阈值（米），`match_dist < 阈值` 才判定空间匹配 |
| `time_gate_ms` | `50.0` | 时间门控（毫秒），RTK 插值时间差超阈值或超出 RTK 采样范围则剔除 |

只有通过二者（即 `is_matched == True`）的帧才会进入分段统计。

### 4.3 距离指标选择

当前固定使用 **`radar_Dx`（雷达纵向距离）** 作为分桶指标。若需改用 RTK 真值纵向距离 `rtk_center_x` 作为分段依据（更贴合"按目标实际距离分段"的语义），只需将 3.2 节代码中的：

```python
mask = (aligned_df['radar_Dx'] >= lo) & (aligned_df['radar_Dx'] < hi)
```

改为：

```python
mask = (aligned_df['rtk_center_x'] >= lo) & (aligned_df['rtk_center_x'] < hi)
```

---

## 5. 调用与接入点

### 5.1 调用位置

`callbacks.py` 的对比运行回调 `on_cmp_run` 中：

```python
bins = get('comparison', {}).get('distance_bins', [])
bin_stats = compute_distance_bin_stats(result['aligned_df'], bins)
bins_html = render_cmp_distance_bins(bin_stats)
```

### 5.2 渲染

`components/stats_panel.py` 的 `render_cmp_distance_bins(bin_stats)` 将结果渲染为 HTML 表格，输出到右侧面板 `cmp-bins-content` 卡片（表头：区间 / 帧数 / RMSE / Mean / Max）。

---

## 6. 维护与扩展建议

- **新增统计量**：在 3.3 节的 `_compute_error_metrics` 中扩展字段，并在 `compute_distance_bin_stats` 的结果 dict 中透出即可，渲染层同步补充列。
- **变阈值分段**：若需支持"阈值随距离变化"（如泊车 KPI 中横向误差 `0.2 + 2.5%·x`），可参考本结构新增独立的 KPI 统计函数，复用 `aligned_df` 与分桶骨架。
- **分段指标可配置化**：当前分桶指标硬编码为 `radar_Dx`。如需灵活切换（雷达/RTK 距离），建议将指标列名作为 `compute_distance_bin_stats` 的参数传入，避免直接改源码。
- **空区间处理**：当前空区间返回全 `None` 占位记录，渲染层需对 `None` 做友好展示（如 `"—"`）。

---

## 7. 关键文件索引

| 文件 | 内容 |
|------|------|
| `radar_wave_analyzer/comparison/alignment.py` | `compute_distance_bin_stats`、`_compute_error_metrics`、`align_trajectories` |
| `radar_wave_analyzer/config.yaml` | `comparison.distance_bins` 配置项 |
| `radar_wave_analyzer/config.py` | `distance_bins` 校验逻辑 |
| `radar_wave_analyzer/callbacks.py` | `on_cmp_run` 中调用与结果分发 |
| `radar_wave_analyzer/components/stats_panel.py` | `render_cmp_distance_bins` 表格渲染 |
| `radar_wave_analyzer/components/layout.py` | 右侧面板 `cmp-bins-content` 卡片定义 |
