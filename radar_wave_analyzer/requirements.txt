# 雷达目标轨迹波动分析系统 — 运行时依赖
pandas>=1.5.0
numpy>=1.23.0
scipy>=1.10.0
plotly>=5.14.0
dash>=2.9.0
dash-bootstrap-components>=1.4.0
flask-caching>=2.0.0
pyyaml>=6.0
pywebview>=5.0
matplotlib>=3.7.0

# ---- 可选依赖（缺失时自动降级，不影响核心功能） ----
# plotly-resampler>=0.9.0   → 大数据量降采样（config.yaml: RESAMPLER_ENABLED）
# 图片导出使用 matplotlib；无需额外安装 kaleido

# ---- 打包依赖（仅构建时需要，运行时不依赖） ----
# pyinstaller>=6.0   → pip install pyinstaller（build_package.ps1 自动安装）
