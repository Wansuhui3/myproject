# 雷达目标与RTK真值数据对齐 — 软件流程与算法详解

> **基于实际数据案例**：C928前向雷达 (FLR) 与 RTK真值  
> **数据文件**：`C928_flr_track_2026_04_10_18_05_07.csv`、`RTK_track_2026_04_10_18_05_07.csv`

---

## 一、软件整体运行流程

用户将两个CSV文件输入软件后，软件按以下流程运行：

```
用户操作                      软件处理                        输出/反馈
────────────────────────────────────────────────────────────────────────

① 选择文件              ② 文件解析与校验
   雷达CSV ──────────→   解析CSV头行，识别字段
   RTK CSV ──────────→   统计行数/ID数/时间范围
                         自动判断哪个是雷达、哪个是RTK
                                │
                                ▼
                    ③ 数据预览与角色确认
                         展示基本信息供用户确认
                         "雷达: 7528行, 15个ID, 20Hz"
                         "RTK: 5987行, 1个ID, 100Hz"
                                │ 用户确认
                                ▼
                    ④ 目标ID自动发现
                         遍历每个雷达ID，计算与RTK的空间重合率
                         ID=36 → 重合率100%  ← 推荐
                         ID=46 → 重合率12%
                         用户选定 track_id = 36
                                │ 用户选定
                                ▼
                    ⑤ 坐标系一致性诊断
                         计算 Δx=Dx-center_x, Δy=Dy-center_y 的均值
                         |mean(Δx)|<0.5m 且 |mean(Δy)|<0.5m → 同系
                                │
                                ▼
                    ⑥ 时间延迟自动检测
                         扫描 -200ms ~ +200ms
                         计算各延迟下的RMSE
                         差异<5% → 推荐 d=0
                                │ 用户确认/微调
                                ▼
                    ⑦ 一键执行对齐
                         Step1: 时间戳解析 + 延迟补偿
                         Step2: RTK线性插值
                         Step3: 逐帧空间匹配
                         Step4: 误差计算与统计
                                │
                                ▼
                    ⑧ 结果展示
                         统计卡片 + 可视化图表 + 分距离区间表
                                │
                                ▼
                    ⑨ 导出结果
                         aligned_result.csv / summary.json / 图表
```

**核心设计原则**：自动检测 + 人工确认 + 过程透明 + 一键执行

---

## 二、数据特征分析

### 2.1 数据基本信息

| 数据源 | 文件 | 角色 | 总行数 | 唯一时间戳 | 不同ID数 | 采样率 |
|--------|------|------|--------|-----------|----------|--------|
| C928前向雷达 | C928_flr_track_*.csv | 待评估数据 | 7528 | 1150 | 15个 | ~20Hz (~50ms/帧) |
| RTK真值 | RTK_track_*.csv | 基准真值 | 5987 | 5987 | 1个 (ID=1) | ~100Hz (~10ms/帧) |

### 2.2 字段对比

**雷达数据关键字段**：

| 字段 | 说明 | 单位 |
|------|------|------|
| timestamp | 时间戳 (YYYY_MM_DD_HH_MM_SS_mmm) | 格式如 `2026_04_10_18_05_07_654` |
| ID | 目标跟踪ID | 整数 |
| Dx, Dy | 目标相对位置（纵向，横向） | 米(m) |
| Vx, Vy | 目标相对速度（纵向，横向） | m/s |

**RTK真值关键字段**：

| 字段 | 说明 | 单位 |
|------|------|------|
| timestamp | 时间戳 (同上格式) | 格式如 `2026_04_10_18_05_07_640` |
| ID | 目标ID（本数据固定为1） | 整数 |
| center_x, center_y | 目标真实位置（纵向，横向） | 米(m) |
| Vx, Vy | 目标真实速度（纵向，横向） | m/s |

### 2.3 为什么需要对齐

雷达和RTK之间存在三个维度的差异：

1. **时间维度**：RTK采样率(100Hz)是雷达(20Hz)的5倍，时间戳不对齐。例如雷达帧 654ms 和 704ms 之间，RTK有640ms, 649ms, 659ms, 670ms, 680ms, 690ms等多个点。
2. **目标维度**：雷达一帧可能检测到多个目标（如ID=36, 46, 47同时出现），RTK只有一个目标（ID=1）。需要确定哪个雷达目标对应RTK真值。
3. **数值维度**：即使时间和目标都匹配，雷达测量值与真值之间必然存在误差，需要量化评估。

---

## 三、软件各步骤算法详解

### 3.1 Step②：文件解析与校验

**目的**：读取CSV文件，自动识别数据角色（雷达/RTK），校验数据完整性。

**识别逻辑**：

```
读取CSV头行
若包含 "Dx" 和 "Dy" 列 → 识别为雷达数据
若包含 "center_x" 和 "center_y" 列 → 识别为RTK真值数据

校验项：
  - 时间戳格式是否为 YYYY_MM_DD_HH_MM_SS_mmm
  - 是否有空值或异常值（如Dx=NaN）
  - 两个文件的时间范围是否有重叠
  - 时间戳末尾是否有空格（实测发现存在）
```

**注意**：实测发现时间戳字段**末尾有空格**（如 `2026_04_10_18_05_07_654 `），必须先 `strip()` 去除空格再解析，否则毫秒段解析错位。

### 3.2 Step③：数据预览

**目的**：向用户展示两个文件的基本统计信息，供用户确认角色分配是否正确。

**展示内容**：

| 信息项 | 雷达 | RTK |
|--------|------|-----|
| 总记录数 | 7528 | 5987 |
| 唯一时间戳数 | 1150 | 5987 |
| 不同ID数 | 15 | 1 |
| 起始时间 | 18:05:07.654 | 18:05:07.640 |
| 结束时间 | 18:06:07.452 | 18:06:07.500 |
| 采样率 | ~20Hz | ~100Hz |

### 3.3 Step④：目标ID自动发现

**目的**：遍历雷达所有ID，找出与RTK真值轨迹空间重合率最高的ID。

**算法**：

```
对于每个雷达ID：
  1. 提取该ID的所有帧
  2. 对每帧计算与RTK最近邻点的欧氏距离
  3. 统计距离 < 5m 的帧占比 → 即为"重合率"

输出：按重合率降序排列
  ID=36 → 重合率 100% (1150/1150帧)  ← 推荐
  ID=46 → 重合率 12%
  ID=47 → 重合率 8%
  ...
```

**为什么不能从全部ID中自动选最近的**：

- 旁车（ID=46）在某些帧可能比主车（ID=36）更靠近RTK
- 车辆切入/切出时，ID切换会导致误匹配
- 跳帧或ID回绕时，算法会错误地"跟丢"目标
- **必须由用户确认**选择哪个ID

### 3.4 Step⑤：坐标系一致性诊断

**目的**：验证雷达Dx/Dy与RTK center_x/center_y是否在同一坐标系下。

**算法**：

```
取稳定跟踪段（前100帧）
计算差值序列：
  Δx_i = Dx_i - center_x_i
  Δy_i = Dy_i - center_y_i

判断：
  若 |mean(Δx)| < 0.5m 且 |mean(Δy)| < 0.5m
    → 同系 ✅，无需坐标变换
  若偏差达米级/百米级
    → 坐标系不同 ❌，必须先做坐标变换
```

**本案例实测**：
- mean(Δx) = +0.25m，mean(Δy) = -0.20m
- 偏差在0.5m以内 → 同系，无需变换
- 微小偏差来自雷达安装点与RTK天线的固定偏移（lever arm）

### 3.5 Step⑥：时间延迟自动检测

**目的**：判断雷达和RTK之间是否存在固定时间延迟，并估计最优延迟值。

#### 3.5.1 什么是时间延迟

时间延迟（Time Offset）是指雷达和RTK两个系统时钟之间的固定偏差。例如：
- 雷达在 `t=654ms` 时刻输出目标位置 `Dx=22.38m`
- 如果雷达存在50ms处理延迟，意味着这个输出实际对应 `t=604ms` 时刻的测量

**延迟来源**：
1. **传感器处理时延**：雷达从采集到输出需要计算时间（目标检测、跟踪滤波）
2. **总线传输延迟**：CAN/LIN等总线传输带来的固定延迟
3. **时钟不同步**：两个系统使用不同的时钟源，存在固定偏差

#### 3.5.2 为什么需要检测延迟

**如果存在显著延迟且不做补偿**：
- 插值时使用的RTK数据是"未来"或"过去"的位置
- 导致位置误差增大，RMSE偏高
- 速度误差也会出现系统性偏差

**本案例的特殊性**：
- 雷达和RTK来自同一录制系统，时钟同步良好
- 实测偏移仅 ±6ms，对结果影响可忽略
- 但对于不同系统采集的数据，延迟可能达到数十毫秒

#### 3.5.3 扫描测试法（主方法）

**原理**：遍历不同假设延迟，找到使RMSE最小的延迟值。

```
算法：
对于每个假设延迟 d ∈ [-200ms, +200ms]，步长10ms：
  1. 将雷达时间戳偏移 d：t_radar' = t_radar + d
  2. 用偏移后的时间戳对RTK做线性插值
  3. 计算所有匹配帧的位置误差 ΔDist(d)
  4. 计算RMSE(d) = √[mean(ΔDist(d)²)]

最优延迟 d_opt = argmin_d RMSE(d)
```

**实际计算示例（本案例数据）**：

| 假设延迟 | ΔDx RMSE (m) | ΔDy RMSE (m) | ΔDist RMSE (m) | ΔVx RMSE (m/s) | 说明 |
|----------|--------------|--------------|----------------|----------------|------|
| 0ms | 0.5907 | 0.3258 | **0.6746** | 0.1128 | 雷达与RTK同步 |
| -50ms | 0.5853 | 0.3248 | **0.6694** | 0.1027 | 本数据最优 |
| -85ms | 0.5879 | 0.3241 | **0.6713** | 0.0963 | CD701雷达典型延迟 |

**分析**：
- 三种延迟的RMSE差异 < 1%（0.6746 vs 0.6694 vs 0.6713）
- -50ms略优，但改善幅度在噪声范围内
- **结论**：本数据不存在显著时间延迟，可假设 `d = 0`

**对比：如果延迟显著会怎样**：

| 假设延迟 | 场景 | ΔDist RMSE (m) |
|----------|------|----------------|
| 0ms（未补偿） | 错误假设 | 1.25 |
| -100ms（正确补偿） | 正确补偿 | **0.68** |
| 改善幅度 | | 45.6% |

可见，若实际有100ms延迟但未补偿，位置RMSE从0.68m恶化到1.25m，误差翻倍。

#### 3.5.4 互相关法（辅助验证）

除了扫描法，还可以用**互相关函数**（Cross-Correlation）估计延迟：

```
使用速度信号做互相关（速度变化明显，互相关更可靠）
雷达速度序列：Vx_radar[1...N]
RTK速度序列：Vx_rtk[1...M]

计算互相关：corr = correlate(Vx_radar, Vx_rtk, mode='full')
找互相关峰值位置：peak_idx = argmax(corr)
最优延迟 = lags[peak_idx]
```

**原理**：互相关峰值对应两个序列最对齐的位置，即时间延迟。

**适用条件**：
- 速度/位置信号需要有明显变化（如加速、减速）
- 匀速直线运动时互相关不明显
- 适合做初步估计，扫描法更精确

#### 3.5.5 延迟补偿策略

根据检测结果决定：

| 延迟范围 | 处理策略 | 说明 |
|----------|----------|------|
| \|d\| < 10ms | 无需补偿 | 插值误差可忽略 |
| 10ms <= \|d\| < 50ms | 建议补偿 | 补偿后可改善5-15%精度 |
| \|d\| >= 50ms | 必须补偿 | 否则误差显著增大 |

**补偿方法**：

```python
# 补偿后的雷达时间戳
t_radar_compensated = t_radar + d_optimal

# 用补偿后的时间戳对RTK插值
rtk_interp = linear_interpolate(rtk_data, t_radar_compensated)
```

#### 3.5.6 延迟检测判断标准

| 场景 | RMSE曲线特征 | 处理方式 |
|------|-------------|----------|
| 曲线平缓（变化<5%） | 延迟不敏感，时钟同步良好 | 假设 d=0 |
| 曲线有明显极小值 | 存在固定延迟 | 采用该极小值对应的延迟 |
| 曲线无明显特征 | 信号变化不明显，检测失败 | 改用扫描法，加大扫描范围 |

### 3.6 Step⑦-1：时间戳解析

**目的**：将字符串格式的时间戳转换为数值型（毫秒），便于计算时间差和排序。

**原始格式**：`YYYY_MM_DD_HH_MM_SS_mmm`（如 `2026_04_10_18_05_07_654`）

**解析方法**：

```
提取各段：year=2026, month=4, day=10, hour=18, minute=5, second=7, ms=654
转换为Unix时间戳（秒）：
T = datetime(2026,4,10,18,5,7) 到 epoch(1970,1,1) 的总秒数 + ms/1000
```

**为什么不用"仅时分秒"计算**：

简单方法 `T = (hour×3600 + minute×60 + second)×1000 + ms` 只算一天内的偏移。对于**单日数据**可行，但：
- 若数据跨天（23:59→00:01），时间戳会突变为0附近，造成错误
- 若多天数据拼接，无法区分不同天的数据

**因此必须使用完整日期时间的epoch秒**，确保跨天场景正确。

### 3.7 Step⑦-2：RTK数据线性插值

**目的**：将高频RTK数据插值到雷达的每个时间戳上，实现时间维度对齐。

**为什么需要插值**：

雷达时间戳 `t_radar` 和 RTK 时间戳 `t_rtk` 不一致。例如：
- 雷达时刻：`654ms`
- RTK时刻：`640ms` (cx=22.31), `649ms` (cx=22.34), `659ms` (cx=22.40)
- 654ms不在RTK数据中，需要估算t=654ms时的RTK位置。

**线性插值原理**：

在RTK时间序列中找到相邻两点 `t_k` 和 `t_{k+1}`，满足 `t_k ≤ t_radar < t_{k+1}`。

对于任意字段 f（如 center_x），线性插值公式：

```
f(t_radar) = f(t_k) + (t_radar - t_k) / (t_{k+1} - t_k) × [f(t_{k+1}) - f(t_k)]
```

**实际计算示例**（以雷达时刻 654ms 为例）：

RTK数据：
- t_k = 649ms, center_x = 22.34m
- t_{k+1} = 659ms, center_x = 22.40m

插值计算：
```
Δt = 654 - 649 = 5ms
Δt_total = 659 - 649 = 10ms
t_ratio = 5 / 10 = 0.5

center_x_interp(654ms) = 22.34 + 0.5 × (22.40 - 22.34)
                       = 22.34 + 0.5 × 0.06
                       = 22.34 + 0.03
                       = 22.37 m
```

**为什么用线性插值而不是"最近邻"**：

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 最近邻 | 计算简单，直接取最接近的RTK点 | 不连续，可能引入量化误差 | 两源时间几乎同步（偏差<2ms） |
| 线性插值 | 连续平滑，更精确 | 需要相邻两点 | 常规场景 |

对于本数据，时钟偏移仅±6ms，两种方法结果差异<0.002m。但线性插值是更规范的解法。

**插值字段**：`center_x`、`center_y`、`Vx`、`Vy`

**边界处理**：
- 左侧（早于RTK起点）：取RTK第一个值
- 右侧（晚于RTK终点）：取RTK最后一个值

**计算复杂度**：O(N_radar + N_rtk)，用双指针遍历实现。

### 3.8 Step⑦-3：目标关联匹配

**目的**：确定每帧中哪个雷达目标对应RTK真值目标。

**前置条件**：已通过Step④确定目标ID（如ID=36），裁剪为单一轨迹。

**匹配算法**：计算雷达目标与RTK插值位置的欧氏距离

```
dist = √[(Dx - cx_interp)² + (Dy - cy_interp)²]
```

**实际计算**（以654ms为例）：

- 目标36：dist = √[(22.38-22.37)² + (-0.12-0.07)²] = √[0.01 + 0.0361] = **0.21m**
- 目标46：dist = √[(15.19-22.37)² + (1.50-0.07)²] = √[51.55 + 2.04] = **7.34m**
- 目标47：dist = √[(25.88-22.37)² + (1.38-0.07)²] = √[12.32 + 1.72] = **3.72m**

最近目标：**ID=36**（0.21m），确认为真值对应目标。

**阈值过滤（空间门控）**：

```
若 dist < 5.0m → 匹配成功
若 dist ≥ 5.0m → 该帧标记为未匹配（雷达丢失目标或RTK目标不在视野）
```

**为什么设5m阈值**：
- 雷达定位精度通常在1-2m，真值目标距离通常在2-50m
- 旁车通常在同一车道上，距离差至少数米
- 5m既能容纳测量误差，又不会误配到其他车道目标

### 3.9 Step⑦-4：精度指标计算

**目的**：量化评估雷达测量相对于真值的误差。

#### 位置误差

| 指标 | 计算公式 | 654ms示例 |
|------|----------|-----------|
| 纵向位置误差 | `ΔDx = Dx_radar - center_x_interp` | 22.38 - 22.37 = **+0.01m** |
| 横向位置误差 | `ΔDy = Dy_radar - center_y_interp` | -0.12 - 0.07 = **-0.19m** |
| 绝对位置误差 | `ΔDist = √(ΔDx² + ΔDy²)` | √(0.01² + 0.19²) = **0.19m** |

#### 速度误差

| 指标 | 计算公式 | 654ms示例 |
|------|----------|-----------|
| 纵向速度误差 | `ΔVx = Vx_radar - Vx_interp` | 4.19 - 4.25 = **-0.06m/s** |
| 横向速度误差 | `ΔVy = Vy_radar - Vy_interp` | -0.25 - (-0.12) = **-0.13m/s** |
| 绝对速度误差 | `ΔV = √(ΔVx² + ΔVy²)` | √(0.06² + 0.13²) = **0.14m/s** |

#### 统计汇总指标

对所有匹配帧计算以下统计量：

| 指标 | 公式 | 含义 |
|------|------|------|
| 均值 (Mean) | `μ = (1/N) Σ e_i` | 系统性偏差（Bias） |
| 标准差 (Std) | `σ = √[(1/N) Σ (e_i - μ)²]` | 误差波动程度 |
| 均方根误差 (RMSE) | `RMSE = √[(1/N) Σ e_i²]` | 综合精度，**最核心指标** |
| 最大绝对误差 (Max) | `Max = max(\|e_i\|)` | 最坏情况误差 |

---

## 四、关键参数说明

| 参数 | 默认值 | 说明 | 调整建议 |
|------|--------|------|----------|
| 时间延迟 d | 自动检测 | 雷达与RTK的固定偏移 | 必须用扫描法实测，不可假设 |
| 目标ID | 自动推荐 | 真值对应的雷达目标ID | 必须用户确认，不可算法自挑 |
| 插值方法 | linear_interp | 线性插值 | 可改为nearest（精度略低） |
| 空间阈值 | 5.0m | 距离超过此值判为未匹配 | 城区3m，高速8m |
| 时间门控 | 50ms | 时间差超过此值判为不重叠 | 时钟好可收紧到20ms |
| 延迟扫描范围 | ±200ms | 测试延迟的范围 | 通常±100ms即可 |
| 延迟扫描步长 | 10ms | 扫描间隔 | 需补偿时加密到1ms |

---

## 五、输出数据格式

对齐结果CSV包含以下字段：

| 字段 | 说明 |
|------|------|
| timestamp | 雷达原始时间戳 |
| radar_ID | 匹配到的雷达目标ID |
| rtk_center_x / rtk_center_y | 插值后RTK位置(m) |
| rtk_Vx / rtk_Vy | 插值后RTK速度(m/s) |
| radar_Dx / radar_Dy | 雷达位置(m) |
| radar_Vx / radar_Vy | 雷达速度(m/s) |
| pos_error_x / pos_error_y | 纵向/横向位置误差(m) |
| pos_error_abs | 绝对位置误差(m) |
| vel_error_x / vel_error_y | 纵向/横向速度误差(m/s) |
| vel_error_abs | 绝对速度误差(m/s) |
| match_dist | 空间匹配距离(m) |

---

## 六、算法原理总结

### 6.1 为什么采用"延迟检测+插值+最近邻"架构

| 挑战 | 解决方案 | 原理 | 为什么用这种算法 |
|------|----------|------|------------------|
| 时钟偏移 | 延迟检测+补偿 | 扫描法找最优延迟使RMSE最小 | 不同系统采集时延迟可达数十ms，必须补偿 |
| 时间不同步 | 线性插值 | RTK高频，可看作连续轨迹，对雷达时刻做插值 | 最近邻简单但不够平滑，插值更精确 |
| 多目标混淆 | 预筛ID + 最近邻 | 先定目标消除歧义，再按空间位置关联 | 直接全局最近邻会被旁车干扰 |
| 测量误差量化 | 误差统计体系 | 逐帧计算残差，用Mean/Std/RMSE综合评价 | 全面反映系统偏差和随机误差 |

### 6.2 算法假设与前提

1. **时间戳格式正确**：`YYYY_MM_DD_HH_MM_SS_mmm`，末尾可能带空格需strip()
2. **延迟可估计**：时钟偏移是固定值（不是时变延迟），可用扫描法/互相关估计
3. **坐标系一致**：RTK和雷达在同一车辆坐标系下，无需坐标变换
4. **目标可识别**：真值目标在雷达视野中，且ID可预先确定

### 6.3 算法局限性

- **时变延迟**：如果延迟随时间变化（如CPU负载波动），固定延迟补偿不够，需要动态对齐
- **坐标系不同**：如果RTK是经纬度或原点不同，必须先做坐标变换（旋转+平移）
- **目标遮挡**：如果目标在视野边缘或远距离遮挡，可能丢失匹配
- **延迟检测失败**：如果雷达和RTK信号变化不明显（匀速直线），互相关法可能失效，需用扫描法

---

## 七、实测结果与验证

### 7.1 延迟检测结果

对 case1 数据，测试三种假设延迟：

| 假设延迟 | ΔDx RMSE (m) | ΔDy RMSE (m) | ΔDist RMSE (m) | 改善幅度 |
|----------|--------------|--------------|----------------|----------|
| 0ms（无延迟） | 0.5907 | 0.3258 | **0.6746** | - |
| -50ms | 0.5853 | 0.3248 | **0.6694** | 0.77% |
| -85ms | 0.5879 | 0.3241 | **0.6713** | 0.49% |

**结论**：三种延迟RMSE差异 < 1%，在噪声范围内，本数据时钟同步良好，假设 d=0 合理。

### 7.2 对齐结果

**匹配情况**：
- 雷达唯一帧数：1150
- 成功配对帧：1150 (100%)
- 未匹配帧：0

**位置误差**：

| 指标 | ΔDx (m) | ΔDy (m) | ΔDist (m) |
|------|---------|---------|-----------|
| Mean | +0.2495 | -0.2037 | 0.5933 |
| Std | 0.5354 | 0.2542 | 0.3210 |
| RMSE | 0.5907 | 0.3258 | 0.6746 |
| Max | 1.8064 | 0.9200 | 2.0050 |

**速度误差**：

| 指标 | ΔVx (m/s) | ΔVy (m/s) | ΔV (m/s) |
|------|-----------|-----------|----------|
| Mean | -0.0955 | -0.0558 | 0.2430 |
| Std | 0.0601 | 0.2476 | 0.1346 |
| RMSE | 0.1128 | 0.2538 | 0.2778 |

### 7.3 交叉验证

| 方法 | ΔDx Bias | ΔDist RMSE | ΔV RMSE | 说明 |
|------|----------|------------|---------|------|
| 线性插值(d=0) | +0.2495 | 0.6746 | 0.2778 | 本算法默认 |
| 最近邻(d=0) | +0.2513 | 0.6741 | 0.2775 | 差异<0.002 |
| 线性插值(d=-50ms) | +0.2461 | 0.6694 | 0.2712 | 扫描法最优 |

**结论**：
1. 本数据时钟同步良好，延迟补偿改善<1%
2. 线性插值与最近邻结果一致，算法实现正确
3. **但对于不同系统采集的数据，必须先做延迟检测**

### 7.4 误差分析

- 纵向位置有+0.25m的系统偏差（雷达偏高），横向有-0.20m偏差（雷达偏左）
- 位置RMSE 0.67m，在1m以内，雷达定位精度满足常规需求
- 速度误差RMSE在0.28m/s以内，纵向精度优于横向
- 这些误差主要来源于雷达安装位置偏差（lever arm）和传感器固有测量误差

---

## 八、总结

本软件通过**九个步骤**完成雷达与RTK真值数据的对齐：

1. **文件选择** → 2. **解析校验** → 3. **预览确认** → 4. **ID发现** → 5. **坐标系诊断** → 6. **延迟检测** → 7. **一键对齐** → 8. **结果展示** → 9. **导出**

核心算法四步：

1. **延迟检测**（扫描法/互相关法）：估计雷达与RTK的固定时间延迟
2. **时间对齐**（线性插值）：将高频RTK数据重采样到雷达时刻
3. **目标关联**（预筛ID + 最近邻）：确定真值对应的雷达目标
4. **误差量化**（Mean/Std/RMSE/Max）：全面评估雷达精度

**对于本案例数据**：时钟同步良好、坐标系一致、100%帧匹配率

**对于其他数据**：必须先做延迟检测，若RMSE改善>5%必须补偿，若坐标系不同必须先变换
