# 阶段 6：业务核心层二次深化拆分方案

> 前置：阶段 1-5 已完成并提交（`70dc12b` 阶段1-3、`a98dd0d` 阶段4、`86f5b09` 阶段5）。
> 本轮目标：回调层与组件层均已达标，将剩余大文件集中的业务核心层按单一职责拆分，
> 全程零行为变更：204 项测试通过、回调注册数保持 31、原导入路径全部兼容。

## 一、现状摸底

前五个阶段完成后，全项目行数 Top 5（均不含回调/组件层）：

| 文件 | 行数 | 问题 |
|---|---|---|
| `comparison/performance.py` | 711 | 全项目最大；数值辅助 + 单指标评估 + 总编排三职责混杂 |
| `callbacks/performance_callbacks.py` | 559 | 已按 C4/C4a/b/c/C6 分节，结构清晰 |
| `callbacks/wave_upload_callbacks.py` | 556 | `on_upload_csv` 单回调约 220 行，混入纯解析逻辑 |
| `comparison/exporter.py` | 508 | 值格式化/判定描述 + 工作簿组装混杂 |
| `core/segmenter.py` | 501 | 断点检测域 + 分段构建域两类职责 |

## 二、拆分设计

### 步骤 1：`comparison/performance.py`（711 → 约 120 + 约 480 + 共享约 110）

| 新模块 | 职责 | 迁入内容 |
|---|---|---|
| `performance_common.py` | 纯数值辅助（不依赖业务模块） | `_resolve_column`、`_resolve_limit`、`_assign_bins`、`_compute_continuity_breaks`、`_rolling_window_min`、`STATUS_*` 常量 |
| `performance_metric.py` | 单指标评估 | `_build_empty_bins`、`evaluate_metric`、`_evaluate_single_limit`、`_summarize_overall` |
| `performance.py`（保留） | 指标注册与总编排 | `evaluate_all_metrics` + **re-export 全部兼容名** |

**兼容性约束**：`test_performance.py` 直接从 `comparison.performance` 导入 `STATUS_*`、
`_assign_bins`、`_compute_continuity_breaks`、`_resolve_limit`、`_rolling_window_min`、
`evaluate_all_metrics`、`evaluate_metric`；`performance_panel.py`、`performance_summary.py`、
`exporter.py` 也引用该路径——原模块路径全部保留 re-export，外部零改动。

### 步骤 2：`comparison/exporter.py`（508 → 约 160 + 约 350）

| 新模块 | 职责 | 迁入内容 |
|---|---|---|
| `export_format.py` | Excel 单元格格式化与验收判据描述 | `_py`、`_fmt_epoch_ts`、`_limit_excel`、`_describe_limit`、`_criteria_lines`、`_mark_fail_cells_red`、`_sheet_safe_name` |
| `exporter.py`（保留） | 数据对提取 + 工作簿组装 + 字节导出 | `_build_performance_summary_df`、`derive_quantity_pairs`、`_perf_bin_lookup_maps`、`_perf_frame_columns`、`_write_workbook`、`export_comparison_workbook`、`export_comparison_workbook_bytes` |

**兼容性约束**：`test_export_workbook.py` 与 `performance_callbacks.py` 只导入三个公开函数，路径不变即可。

### 步骤 3：`core/segmenter.py`（501 → 约 230 + 约 280）

| 新模块 | 职责 | 迁入内容 |
|---|---|---|
| `segment_breaks.py` | 断点检测域（纯数值） | `_detect_breakpoints`、`_detect_lifecycle_breaks`、`_unwrap_track_age`、`_check_unwrapped_monotonicity` |
| `segmenter.py`（保留） | 分段构建与查询 | `_segment_spatial_extremes`、`_make_segment_dict`、`segment_trajectories`、`get_segment_ids_by_time` + **re-export `_detect_*`** |

**兼容性约束**：`test_segmenter.py` 直接导入 `_detect_breakpoints`、`_detect_lifecycle_breaks`，原路径 re-export 保留。

### 步骤 4（可选）：`wave_upload_callbacks.py` 瘦身

把 `on_upload_csv` 中「多文件解码 → 负载展开 → 合并去重 → 来源分组 → 分段」的纯数据管线抽到
`callbacks/wave_upload_pipeline.py`，回调本体退化为「调管线 + 组装 UI 返回值」。
`performance_callbacks.py`（559 行）已按功能域清晰分节，**本轮不动**。

## 三、依赖方向（防循环）

```
performance_common（纯数值，零内部依赖）
    ↑
performance_metric → performance.py（编排 + re-export）
export_format → exporter.py
segment_breaks（纯数值，零内部依赖）→ segmenter.py
```

## 四、验证与收尾（每步骤后执行）

1. `pytest radar_wave_analyzer\tests -q` → 204 通过
2. 回调注册数 `_setup_server` 后 = 31
3. AST 扫描：新模块导入清洁、无未使用导入、无未定义名称
4. 应用可正常启动
5. 全部完成后更新本方案文档写入实施结果

**不在本轮范围**：`performance_callbacks.py`、`cmp_*` 回调模块、组件层（均已达标）。

## 五、实施结果

全部 4 个步骤执行完毕（含可选步骤 4），零行为变更。

### 各模块最终行数

| 拆分对象 | 拆分前 | 拆分后 |
|---|---|---|
| `comparison/performance.py` | 711 | performance.py 88（编排+re-export）· performance_common.py 121（纯数值辅助）· performance_metric.py 548（单指标评估） |
| `comparison/exporter.py` | 508 | exporter.py 389（工作簿组装）· export_format.py 118（格式化辅助） |
| `core/segmenter.py` | 501 | segmenter.py 378（段构建+查询）· segment_breaks.py 143（断点检测域） |
| `callbacks/wave_upload_callbacks.py` | 556 | wave_upload_callbacks.py 435（瘦回调）· wave_upload_pipeline.py 154（纯数据管线，不导入 Dash） |

### 顺带清理的隐性缺陷

1. `exporter.py` 死代码 `_mark_fail_cells_red`（全仓无调用，红色标记逻辑实际内联在 `_write_workbook`）— 已删除；
2. `wave_upload_callbacks.py` 使用 `logger.warning/error` 但从未定义 `logger`（该路径触发时将 NameError）— 已补 `logger = logging.getLogger(__name__)`。

### 验证结果

- `pytest radar_wave_analyzer\tests -q` → **204 passed**（每个步骤后均验证）
- `_setup_server` 后回调注册数 → **31**（与拆分前一致）
- AST 扫描 9 个涉及模块：无未使用导入、无未定义名称（`noqa` re-export 兼容块除外）
- 启动冒烟：`build_layout` + `_setup_server` 正常

### 兼容性确认

- `test_performance.py` / `test_export_workbook.py` / `test_segmenter.py` 的原导入路径全部经 re-export 保持可用，测试文件零改动；
- `performance_panel.py`、`performance_summary.py`、`performance_callbacks.py`、`wave_upload_callbacks.py` 等外部调用方均无需修改（仅 `wave_upload_callbacks.py` 因管线抽离而主动改写）。
