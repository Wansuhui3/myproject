# 阶段 8：拆分收官方案

> 前置：阶段 7 已提交（`df7d951`）。
> 目标：处理最后两个超过 400 行的文件，全程零行为变更：
> 204 项测试通过、回调注册数 31、测试文件零改动。
> **本阶段完成后，阶段 1-8 结构性拆分系列收官。**

## 一、现状摸底

| 文件 | 行数 | 拆分缝 |
|---|---|---|
| `comparison/performance_metric.py` | 548 | `_evaluate_single_limit`（L416-543，128 行）是独立整函数，仅被本模块 `evaluate_metric` 调用 |
| `callbacks/wave_upload_callbacks.py` | 435 | `[2] 时间戳筛选 + [Qdyn] 物理量刷新 + [Wclear] 一键清除`（L269-456，约 190 行）与数据装载域职责可分 |
| 其余全部 | ≤ 400 | 已达标（parser 400 / service 399 / exporter 389 / matching 387） |

## 二、拆分设计

### 步骤 1：`comparison/performance_metric.py`（548 → 2 模块）

| 模块 | 职责 | 内容 | 预计行数 |
|---|---|---|---|
| `performance_single_limit.py`（新建） | 单限值模式评估（Ay 类） | 整函数搬移 `_evaluate_single_limit` | ~135 |
| `performance_metric.py`（保留） | 分区间模式 | `_build_empty_bins`、`evaluate_metric`（343 行线性管线，**不再内拆**）、`_summarize_overall` | ~420 |

兼容性：唯一外部引用 `performance.py` 的 re-export 导入块，经
`performance_metric` 中转导入后原路径不变；测试均从
`comparison.performance` 导入，零改动。
明确不做：`evaluate_metric` 内部按阶段抽 helper 属于非机械重构，
数值路径风险高、收益低，维持现状。

### 步骤 2：`callbacks/wave_upload_callbacks.py`（435 → 2 模块）

| 模块 | 职责 | 内容 | 预计行数 |
|---|---|---|---|
| `wave_query_callbacks.py`（新建） | 查询与视图域 | `[2] on_timestamp_input`、`[Qdyn] refresh_quantity_options`、`[Wclear] on_wave_clear` | ~200 |
| `wave_upload_callbacks.py`（保留） | 数据装载域 | `[0a/0b] 雷达切换`、`[1] on_upload_csv`（管线已在阶段 6 抽出） | ~250 |

兼容性：两模块均为回调注册模块，更新 `callbacks/__init__.py` 导入列表
与 docstring；无外部符号引用，测试零改动。

## 三、依赖方向（防循环）

```
performance_common（既有，纯数值）
    ↑
performance_single_limit → performance_metric → performance.py（编排 + re-export）
wave_query_callbacks / wave_upload_callbacks（互不依赖，共享 wave_helpers / wave_views）
```

## 四、验证与收尾

1. 每步骤后 `pytest` → 204 通过；`_setup_server` 后回调数 = 31
2. 依赖图环检测 + 启动冒烟（沿用阶段 7 方法）
3. 方案文档回填实施结果
4. **收官声明**：阶段 8 完成后全项目所有源码文件 ≤ 约 430 行，
   阶段 1-8 结构性拆分系列完结；后续结构调整仅随新功能按既有
   领域模块归置，不再单独立阶段。

## 五、实施结果

全部完成，零行为变更。前置：阶段 7 已提交（`df7d951`）。

### 步骤 1：comparison/performance_metric.py（548 → 2 模块）

| 模块 | 行数 | 职责 |
|---|---|---|
| `performance_single_limit.py`（新建） | 132 | `_evaluate_single_limit` 单限值模式（Ay 类）整函数搬移 |
| `performance_metric.py`（保留） | 431 | `_build_empty_bins`、`evaluate_metric`（343 行线性管线，未内拆）、`_summarize_overall` |

`performance_metric` 中转导入 `_evaluate_single_limit`，`performance.py`
re-export 路径不变，测试文件零改动。

### 步骤 2：callbacks/wave_upload_callbacks.py（435 → 2 模块）

| 模块 | 行数 | 职责 |
|---|---|---|
| `wave_query_callbacks.py`（新建） | 221 | `[2] on_timestamp_input`、`[Qdyn] refresh_quantity_options`、`[Wclear] on_wave_clear` |
| `wave_upload_callbacks.py`（保留） | 229 | `[0a/0b] 雷达切换`、`[1] on_upload_csv`（保留层导入已按需裁剪） |

`callbacks/__init__.py` 导入列表与 docstring 已更新；两模块互不依赖。

### 最终验证

- `pytest radar_wave_analyzer\tests -q` → **204 passed**（每步骤后均验证）
- 回调注册数（`_setup_server` 后）→ **31**，不变
- 依赖图环检测（Tarjan SCC，83 个内部模块）→ **0 循环依赖**
- 启动冒烟 → `build_layout` 渲染正常

### 收官声明

阶段 8 完成后全项目所有源码文件 ≤ 约 430 行
（最大：`performance_metric.py` 431 行），阶段 1-8 结构性拆分系列完结。
后续结构调整仅随新功能按既有领域模块归置，不再单独立阶段。
