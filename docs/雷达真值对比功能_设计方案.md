# 雷达目标与RTK真值对比功能 — 设计方案 v2

> 基于《雷达与RTK真值对齐算法详解.md》中的算法流程设计  
> 布局完全对标现有波动分析界面，最大化复用已有组件和样式

---

## 一、界面布局总览

### 1.1 对标当前布局

| 区域 | 现有波动分析 | 真值对比（新增） | 复用 |
|------|-------------|-----------------|------|
| 顶部栏 | 雷达切换按钮 | **模式切换 Tab**（波动分析 \| 真值对比）+ 状态 | 仅复用 CSS 类 |
| 左侧面板(2/12) | 时间筛选 + ID列表 + 轨迹段 | **上传卡 + 预览卡 + 对齐配置卡** | `app-card` 类 |
| 中间左侧(2/12) | 物理量 Checklist | **对比指标 Checklist** | `qty-checklist` 类，`_build_quantity_checklist` 模式 |
| 中间右侧(10/12) | 多子图+标题栏 | **对比子图+标题栏** | `center-graph-wrapper`、`graph-title-bar` |
| 右侧面板(3/12) | 全段统计 + 框选统计 + 导出 | **误差统计 + 距离区间表 + 导出** | `stats-card`、`stat-row`、`export-card`、`_stat_row()` |

### 1.2 整体布局图

```
┌──────────────────────────────────────────────────────────────────┐
│  ■ 雷达波动分析    □ 真值对比       ← Tab切换 + 状态标签          │
├──────────┬─────────────────────────────────┬──────────────────────┤
│ 左侧 2/12│         中间 7/12               │      右侧 3/12       │
├──────────┼──────┬──────────────────────────┼──────────────────────┤
│          │ 指标 │      图表主区域           │ [误差统计卡片]       │
│ [上传卡] │ 多选 │                           │                      │
│ 雷达CSV  │ 2/12 │  ┌─────────────────────┐ │ ΔDx RMSE  0.5907    │
│ RTK CSV  │      │  │ 对比图表标题栏       │ │ ΔDy RMSE  0.3258    │
│          │ ΔDx  │  │ ID=36 | 1150帧匹配   │ │ ΔDist RMSE 0.6746   │
│ [预览卡] │ ΔDy  │  ├─────────────────────┤ │ ΔVx RMSE  0.1128    │
│ 雷达7528 │ ΔDist│  │                     │ │ ΔVy RMSE  0.2538    │
│ RTK 5987 │ ΔVx  │  │  Dx vs center_x    │ │ ΔV  RMSE  0.2778    │
│ 重叠率100%│ ΔVy  │  │  (雷达线 + RTK线)  │ │                      │
│          │ ΔV   │  │                     │ │ [距离区间表]         │
│ [配置卡] │      │  ├─────────────────────┤ │ 0-10m  200帧 0.52m  │
│ ID=36✓  │      │  │  Dy vs center_y    │ │ 10-20m 350帧 0.65m  │
│ 延迟 0ms│      │  │  (雷达线 + RTK线)  │ │ 20-30m 300帧 0.71m  │
│ 坐标系✓ │      │  │                     │ │ ...                 │
│ [执行对齐]│     │  ├─────────────────────┤ │                      │
│          │      │  │  ΔDx 误差散点      │ │ [导出]               │
│          │      │  │  (带零线)          │ │ 导出CSV 导出图表     │
│          │      │  │                     │ │                      │
│          │      │  └─────────────────────┘ │                      │
└──────────┴──────┴──────────────────────────┴──────────────────────┘
```

---

## 二、目录与文件规划

### 2.1 新增文件

```
radar_wave_analyzer/
├── comparison/                    # ★ 新增目录
│   ├── __init__.py
│   ├── parser.py                  # 文件解析与角色识别（Step②③）
│   ├── id_finder.py               # 目标ID自动发现（Step④）
│   ├── coord_diag.py              # 坐标系一致性诊断（Step⑤）
│   ├── delay_detect.py            # 时间延迟检测（Step⑥）
│   ├── alignment.py               # 核心对齐算法（Step⑦）
│   └── exporter.py                # 对齐结果导出（Step⑨）
│
├── components/
│   └── comparison_charts.py       # ★ 新增：对比图表构建（复用 graph_builder 模式）
```

**说明**：预览、统计面板、ID选择列表、延迟曲线等的 HTML 渲染逻辑直接放在 `callbacks.py` 或 `comparison/` 中，不额外创建 layout 文件（保持轻量）。

### 2.2 需修改的文件

| 文件 | 变更 | 行数估计 |
|------|------|----------|
| `config.yaml` | 末尾追加 `comparison:` 配置节 | +30行 |
| `components/layout.py` | 顶部新增模式切换 Tab；在底部新增对比布局 div（`style='display:none'`） | +60行 |
| `components/stats_panel.py` | 末尾追加对比统计渲染函数 | +50行 |
| `components/graph_builder.py` | 末尾追加对比图表构建函数 | +80行 |
| `components/comparison_charts.py` | 新建：对比专用图表 | +180行 |
| `callbacks.py` | 末尾追加模式切换回调 + 对比功能8个回调 | +500行 |
| `cache.py` | 末尾追加对比数据缓存方法 | +30行 |
| `assets/custom.css` | 末尾追加对比样式 | +100行 |

---

## 三、config.yaml 扩展

```yaml
# ======== 在 config.yaml 末尾追加 ========
comparison:
  # 空间匹配阈值 (米)
  match_threshold: 5.0
  # 时间门控 (毫秒)
  time_gate_ms: 50  
  # 延迟扫描范围 (毫秒)
  delay_scan_range: [-200, 200]
  # 延迟扫描步长 (毫秒)
  delay_scan_step: 10
  # 延迟不敏感阈值 — RMSE变化低于此比例视为延迟不敏感
  delay_insensitive_ratio: 0.05
  # 坐标系一致性阈值 (米)
  coord_bias_threshold: 0.5
  # 分距离区间统计边界 (米)
  distance_bins: [0, 10, 20, 30, 40, 50, 80, 120]
  # 导出编码
  export_encoding: "utf-8-sig"
  # 默认对比指标
  default_quantities:
    - pos_error_x
    - pos_error_y
    - pos_error_abs
  # 对比指标定义 — 格式与 quantities 节一致，可复用 _stat_row / Checklist 渲染
  quantities:
    pos_error_x:
      label: "纵向位置误差 ΔDx"
      unit: "m"
      short_label: "ΔDx"
    pos_error_y:
      label: "横向位置误差 ΔDy"
      unit: "m"
      short_label: "ΔDy"
    pos_error_abs:
      label: "绝对位置误差 ΔDist"
      unit: "m"
      short_label: "ΔDist"
    vel_error_x:
      label: "纵向速度误差 ΔVx"
      unit: "m/s"
      short_label: "ΔVx"
    vel_error_y:
      label: "横向速度误差 ΔVy"
      unit: "m/s"
      short_label: "ΔVy"
    vel_error_abs:
      label: "绝对速度误差 ΔV"
      unit: "m/s"
      short_label: "ΔV"
    match_dist:
      label: "匹配距离"
      unit: "m"
      short_label: "Dist"
```

---

## 四、复用清单（详细）

### 4.1 直接复用的 CSS 类

| CSS 类 | 用途 | 对比中何处使用 |
|--------|------|---------------|
| `.app-card` | 白色卡片容器 | 左侧上传/预览/配置卡、右侧统计卡 |
| `.app-card-title` | 卡片标题 | 所有卡片标题 |
| `.stats-card` | 统计卡片 | 右侧误差统计卡 |
| `.stats-card-title` | 统计卡片标题 | 全段误差统计、距离区间表 |
| `.stats-empty` | 空状态占位 | 统计卡未执行对齐时 |
| `.stats-compact-list` | 紧凑统计列表 | 误差统计行容器 |
| `.stat-row` | 统计行（label+value） | 每条误差指标 |
| `.stat-row-label` | 统计行标签 | ΔDx RMSE 等 |
| `.stat-row-value` | 统计行数值 | 0.5907 等 |
| `.export-card` | 导出卡片 | 导出区域 |
| `.export-btn` | 导出按钮 | 导出CSV、导出图表 |
| `.feedback-muted` | 灰色提示文字 | 上传反馈 |
| `.feedback-info` | 蓝色信息文字 | 执行状态 |
| `.feedback-success` | 绿色成功文字 | 对齐完成提示 |
| `.feedback-error` | 红色错误文字 | 校验错误 |
| `.center-graph-wrapper` | 图表外层容器 | 对比图表容器 |
| `.graph-title-bar` | 图表标题栏 | 对比结果标题 |
| `.id-list-item` | ID列表项 | 目标ID选择列表 |
| `.id-list-item.selected` | 选中ID高亮 | 用户选中的ID |
| `.qty-checklist` | 多选列表容器 | 对比指标多选 |
| `.qty-checklist-label` | 多选标签 | 对比指标标签 |
| `.qty-panel` | 多选面板 | 左侧指标面板 |
| `.badge` | 徽章 | 匹配率/帧数等 |
| `.upload-zone` / `.upload-zone-inner` / `.upload-zone-icon` / `.upload-zone-text` / `.upload-zone-hint` | 上传区 | 双文件上传区 |
| `.top-bar-tag` | 顶部状态标签 | 模式标识 |

### 4.2 直接复用的 Python 函数

| 函数 | 来源 | 对比中如何使用 |
|------|------|---------------|
| `_stat_row(label, value, unit)` | `stats_panel.py` | 渲染每条误差统计行，如 `_stat_row('ΔDist RMSE', '0.6746', 'm')` |
| `_fmt_val(val, decimals)` | `stats_panel.py` | 格式化数值 |
| `_render_empty(title, hint)` | `stats_panel.py` | 未对齐时的占位卡片 |
| `_build_quantity_checklist()` | `layout.py` | 模式：读取 config 构建 Checklist |
| `_wrap_with_resampler(fig, n_points)` | `graph_builder.py` | 大数据降采样包装 |
| `_compute_subplot_y_domains(n)` | `graph_builder.py` | 子图纵向布局 |
| `get(segment)` | `cache.py` | 缓存读写模式 |
| `has_data_loaded()` | `cache.py` | 检查数据是否就绪 |

### 4.3 复用的布局模式

```
dbc.Row([                      # 三栏
    dbc.Col([...], width=2),   # 左侧面板
    dbc.Col([...], width=7),   # 中间面板（内嵌 2+10 子布局）
    dbc.Col([...], width=3),   # 右侧面板
], className='g-0')

dcc.Store(id='cmp-xxx', ...)   # 状态存储

dcc.Loading(id='loading-xxx',  # 加载动画
            type='circle', color='#3b82f6', ...)
```

---

## 五、核心模块详细设计

### 5.1 `comparison/parser.py` — 文件解析与角色识别

```python
def detect_file_role(df: pd.DataFrame) -> str:
    """
    自动识别CSV角色
    - 包含 Dx,Dy 列 → 'radar'
    - 包含 center_x,center_y 列 → 'rtk'  
    - 否则 → 'unknown'
    """

def parse_timestamp(ts_str: str) -> float:
    """
    解析 YYYY_MM_DD_HH_MM_SS_mmm 格式
    注意: 先 strip() 去除首尾空格
    → 返回 epoch 秒 (float)
    """

def load_csv_file(file_bytes: bytes, filename: str) -> dict:
    """
    加载CSV文件，返回:
    {
        'role': 'radar' | 'rtk',
        'df': DataFrame（已解析时间戳，已排序）,
        'filename': str,
        'total_rows': int,
        'unique_timestamps': int,
        'unique_ids': int,
        'time_range': (start_epoch, end_epoch),
        'sample_rate_hz': float,
        'errors': list[str],
    }
    """
```

### 5.2 `comparison/id_finder.py` — 目标ID自动发现

```python
def compute_overlap_rate(
    radar_df, rtk_df, track_id: int, threshold_m: float = 5.0
) -> dict:
    """
    计算某个雷达ID与RTK轨迹的空间重合率
    
    算法（来自文档 Step④）:
      对于该ID的每一帧，找最近的RTK点
      dist = √((Dx-center_x)² + (Dy-center_y)²)
      统计 dist < threshold 的帧占比
    
    返回: {track_id, total_frames, matched_frames, overlap_rate, mean_distance}
    """

def discover_best_id(radar_df, rtk_df) -> list[dict]:
    """遍历所有雷达ID，返回按重合率降序排列的列表"""
```

### 5.3 `comparison/coord_diag.py` — 坐标系诊断

```python
def diagnose_coordinate_system(
    radar_df, rtk_df, track_id: int, n_frames: int = 100
) -> dict:
    """
    算法（文档 Step⑤）:
      取前 n_frames 帧
      Δx = Dx - nearest(center_x), Δy = Dy - nearest(center_y)
      若 |mean(Δx)| < 0.5m 且 |mean(Δy)| < 0.5m → 同系
    
    返回: {same_system, mean_dx, mean_dy, std_dx, std_dy, diagnosis}
    """
```

### 5.4 `comparison/delay_detect.py` — 时间延迟检测

```python
def scan_delay(
    radar_df, rtk_df, track_id: int,
    delay_range: tuple = (-200, 200), step_ms: int = 10
) -> dict:
    """
    扫描法检测最优时间延迟（文档 Step⑥）
    
    对于 d ∈ [-200, +200], step=10ms:
      1. 雷达时间戳偏移 d/1000
      2. 对RTK线性插值
      3. 计算 ΔDist RMSE(d)
    d_opt = argmin(RMSE)
    
    返回: {optimal_delay_ms, min_rmse, delay_curve[(d,rmse),...], 
           delay_insensitive, recommendation}
    """
```

### 5.5 `comparison/alignment.py` — 核心对齐算法

```python
def align_trajectories(
    radar_df, rtk_df, track_id: int,
    delay_ms: float = 0.0,
    match_threshold_m: float = 5.0,
    time_gate_ms: float = 50.0
) -> dict:
    """
    核心对齐算法 — 一次调用完成全部4步（文档 Step⑦）
    
    Step1: 时间戳解析 + 延迟补偿
        t_compensated = t_radar + delay_ms / 1000
    
    Step2: RTK线性插值
        对 center_x, center_y, Vx, Vy 逐字段插值
        使用 numpy.interp, 边界外推取首/末值

    Step3: 逐帧空间匹配
        dist = √((Dx-cx_interp)² + (Dy-cy_interp)²)
        dist < 5m → 匹配成功

    Step4: 误差计算
        pos_error_x = Dx - center_x_interp
        pos_error_y = Dy - center_y_interp
        pos_error_abs = √(pos_error_x² + pos_error_y²)
        vel_error_x = Vx - Vx_interp
        vel_error_y = Vy - Vy_interp
        vel_error_abs = √(vel_error_x² + vel_error_y²)
    
    返回:
    {
        'aligned_df': pd.DataFrame,   # 逐帧对齐结果
        'summary': dict,              # 汇总统计:
        #   pos_error_x: {mean, std, rmse, max, p50, p95}
        #   pos_error_y: {...}, pos_error_abs: {...}
        #   vel_error_x: {...}, vel_error_y: {...}, vel_error_abs: {...}
        'match_summary': dict,        # 匹配概况
        #   {total_frames, matched_frames, match_rate}
    }
    """
```

### 5.6 `components/comparison_charts.py` — 对比图表

```python
def build_comparison_subplots(
    aligned_df: pd.DataFrame,
    selected_quantities: list[str],
    quantities_config: dict,
    trajectory_label: str = '',
) -> go.Figure:
    """
    构建对比多子图（纵向堆叠，共享X轴）
    
    每个选中的对比指标一个子图：
    - pos_error_x / pos_error_y: 叠加显示雷达曲线 + RTK曲线
    - pos_error_abs: 误差散点 + 零线
    - vel_error_x / vel_error_y: 叠加显示雷达曲线 + RTK曲线  
    - vel_error_abs: 误差散点 + 零线
    - match_dist: 匹配距离散点 + 阈值线
    
    复用 _compute_subplot_y_domains, _wrap_with_resampler
    复用颜色方案、子图标注样式
    """

def build_delay_curve(delay_results: dict) -> go.Figure:
    """
    延迟扫描曲线
    x轴: 延迟(ms), y轴: RMSE(m)
    竖虚线标记最优值
    """

def build_error_histogram(aligned_df: pd.DataFrame) -> go.Figure:
    """
    ΔDist 误差分布直方图
    竖虚线标记 mean / p95
    """
```

---

## 六、布局代码设计（可直接嵌入 layout.py）

### 6.1 模式切换 Tab（顶部栏扩展）

```python
# layout.py 的 _build_top_bar() 之后新增

def _build_mode_tabs() -> html.Div:
    """模式切换 Tab 栏。"""
    return html.Div([
        html.Button('雷达波动分析', id='mode-tab-wave', 
                    className='mode-tab active'),
        html.Button('真值对比', id='mode-tab-compare',
                    className='mode-tab'),
        html.Span(id='cmp-status-tag', className='top-bar-tag',
                  style={'marginLeft': 'auto'}),
    ], className='mode-tabs')
```

### 6.2 左侧面板（对比专用）

```python
def _build_cmp_left_panel() -> dbc.Col:
    """左侧面板：上传卡 + 预览卡 + 对齐配置卡。"""
    return dbc.Col([
        # 卡片1：双文件上传
        html.Div([
            html.Div('数据文件', className='app-card-title'),
            html.Div([
                # 雷达CSV上传
                dcc.Upload(
                    id='cmp-upload-radar',
                    accept='.csv', multiple=False, max_size=-1,
                    children=html.Div([
                        html.Div('⬆', className='upload-zone-icon'),
                        html.Div('上传雷达CSV', className='upload-zone-text'),
                        html.Div('需包含 Dx/Dy 列', className='upload-zone-hint'),
                    ], className='upload-zone-inner'),
                    className='upload-zone', style={'marginBottom': '6px'},
                ),
                # RTK CSV上传
                dcc.Upload(
                    id='cmp-upload-rtk',
                    accept='.csv', multiple=False, max_size=-1,
                    children=html.Div([
                        html.Div('⬆', className='upload-zone-icon'),
                        html.Div('上传RTK真值CSV', className='upload-zone-text'),
                        html.Div('需包含 center_x/center_y 列', className='upload-zone-hint'),
                    ], className='upload-zone-inner'),
                    className='upload-zone',
                ),
                html.Div(id='cmp-upload-feedback', className='feedback-muted mt-2'),
            ]),
        ], className='app-card'),

        # 卡片2：数据预览
        html.Div(id='cmp-preview-card', className='app-card', children=[
            html.Div('数据预览', className='app-card-title'),
            html.Div('请先上传两个CSV文件', className='stats-empty'),
        ]),

        # 卡片3：对齐配置
        html.Div(id='cmp-config-card', className='app-card', children=[
            html.Div('对齐配置', className='app-card-title'),
            html.Div([
                # 目标ID选择
                html.Div('目标ID', style={'fontSize':'12px','color':'#64748b','marginBottom':'4px'}),
                html.Div(id='cmp-id-list', className='id-list', children=[
                    html.Div('上传后自动发现', className='id-list-item',
                             style={'cursor':'default','color':'#94a3b8'}),
                ]),
                # 延迟值
                html.Div('时间延迟', style={'fontSize':'12px','color':'#64748b','marginTop':'8px','marginBottom':'4px'}),
                dcc.Input(id='cmp-delay-input', type='number', value=0, min=-200, max=200,
                          className='form-control form-control-sm',
                          style={'width':'100px'}),
                html.Span(' ms', style={'fontSize':'12px','color':'#94a3b8','marginLeft':'4px'}),
                html.Div(id='cmp-delay-feedback', className='feedback-muted mt-1'),
                # 坐标系诊断
                html.Div(id='cmp-coord-diag', className='feedback-muted mt-1'),
                # 执行按钮
                html.Button('执行对齐', id='cmp-run-btn', n_clicks=0,
                            className='export-btn', style={'marginTop':'12px'}),
                html.Div(id='cmp-run-feedback', className='feedback-muted mt-1'),
            ]),
        ]),
    ], width=2, className='p-2', style={'overflowY':'auto','maxHeight':'calc(100vh - 56px)'})
```

### 6.3 中间面板（对比专用）

```python
def _build_cmp_center_panel() -> dbc.Col:
    """中间面板：对比指标多选 + 图表。"""
    cmp_qties = get('comparison', {}).get('quantities', {})
    default_qties = get('comparison', {}).get('default_quantities', ['pos_error_abs'])
    
    options = [{'label': info['label'], 'value': qty}
               for qty, info in cmp_qties.items()]

    return dbc.Col([
        dbc.Row([
            # 左窄：对比指标多选 — 复用 qty-panel / qty-checklist 样式
            dbc.Col([
                html.Div([
                    html.Div('对比指标', className='app-card-title'),
                    dcc.Checklist(
                        id='cmp-quantity-checklist',
                        options=options,
                        value=default_qties,
                        className='qty-checklist',
                        labelClassName='qty-checklist-label',
                    ),
                ], className='app-card qty-panel'),
            ], width=2, style={'paddingRight':'8px'}),

            # 右宽：对比图表 — 复用 center-graph-wrapper / graph-title-bar
            dbc.Col([
                html.Div([
                    html.Div(id='cmp-graph-title', className='graph-title-bar',
                             children=[html.Span('请上传雷达与RTK数据并执行对齐',
                                                  className='feedback-muted')]),
                    dcc.Loading(
                        id='cmp-loading-graph', type='circle', color='#3b82f6',
                        parent_className='loading-graph-inner',
                        children=dcc.Graph(
                            id='cmp-graph', config={
                                'displayModeBar': True,
                                'displaylogo': False,
                                'modeBarButtons': [
                                    ['pan2d'],
                                    ['zoomIn2d','zoomOut2d','autoScale2d','resetScale2d'],
                                ],
                                'responsive': False, 'scrollZoom': True,
                                'doubleClick': 'reset+autosize',
                            },
                            style={'width':'100%','height':'100%'},
                        ),
                    ),
                ], className='center-graph-wrapper'),
            ], width=10, style={'paddingLeft':'8px'}),
        ], className='g-0', style={'flex':'1','minHeight':'0'}),
    ], width=7, className='center-panel')
```

### 6.4 右侧面板（对比专用）

```python
def _build_cmp_right_panel() -> dbc.Col:
    """右侧面板：误差统计 + 距离区间表 + 导出。"""
    return dbc.Col([
        # 误差统计 — 复用 stats-card / stat-row
        dcc.Loading(
            id='cmp-loading-stats', type='circle', color='#3b82f6',
            children=html.Div([
                html.Div('误差统计', className='stats-card-title'),
                html.Div('执行对齐后显示', className='stats-empty'),
            ], id='cmp-stats-content', className='stats-card'),
        ),

        # 分距离区间统计 — 复用 stats-card
        dcc.Loading(
            id='cmp-loading-bins', type='circle', color='#3b82f6',
            children=html.Div([
                html.Div('分距离区间统计', className='stats-card-title'),
                html.Div('执行对齐后显示', className='stats-empty'),
            ], id='cmp-bins-content', className='stats-card'),
        ),

        # 导出 — 复用 export-card / export-btn
        html.Div([
            html.Div('导出', className='app-card-title'),
            html.Button('导出 CSV', id='cmp-export-csv-btn', n_clicks=0,
                        className='export-btn'),
            html.Button('导出图表', id='cmp-export-img-btn', n_clicks=0,
                        className='export-btn'),
            html.Div(id='cmp-export-feedback', className='feedback-muted mt-2'),
        ], className='export-card'),
    ], width=3, className='p-3', style={'overflowY':'auto','maxHeight':'calc(100vh - 56px)'})
```

### 6.5 总布局集成

```python
# layout.py 的 build_layout() 修改：
# 1. 在 _build_top_bar() 下方插入 _build_mode_tabs()
# 2. 现有三栏布局放在 html.Div(id='panel-wave') 内（默认显示）
# 3. 新增对比三栏放在 html.Div(id='panel-compare', style={'display':'none'}) 内

def build_layout() -> html.Div:
    """构建完整布局。"""
    default_qty = get('DEFAULT_QUANTITY', 'Dx')

    return html.Div([
        # ===== 状态存储 =====
        dcc.Store(id='store-data-loaded', data=False),
        dcc.Store(id='store-segments-meta', data=None),
        dcc.Store(id='store-selected-trajectory', data=None),
        dcc.Store(id='store-selected-quantities', data=[default_qty]),
        dcc.Store(id='store-box-selection', data=None),
        dcc.Store(id='store-selected-id', data=None),
        html.Div(id='scroll-anchor', style={'display':'none'}),
        # 对比功能状态（新增）
        dcc.Store(id='cmp-files-loaded', data=False),
        dcc.Store(id='cmp-selected-id', data=None),
        dcc.Store(id='cmp-alignment-done', data=False),

        # ===== 顶部栏 =====
        _build_top_bar(),

        # ===== 模式切换 Tab（新增） =====
        _build_mode_tabs(),

        # ===== 波动分析面板（已有） =====
        html.Div([
            dbc.Row([
                _build_left_panel(),
                _build_center_panel(),
                _build_right_panel(),
            ], className='g-0'),
        ], id='panel-wave'),

        # ===== 真值对比面板（新增，默认隐藏） =====
        html.Div([
            dbc.Row([
                _build_cmp_left_panel(),
                _build_cmp_center_panel(),
                _build_cmp_right_panel(),
            ], className='g-0'),
        ], id='panel-compare', style={'display':'none'}),
    ], className='app-shell')
```

---

## 七、右侧统计面板设计（复用 _stat_row）

### 7.1 误差统计卡片

复用 `stats_panel.py` 的 `_stat_row(label, value, unit)` 渲染：

```
┌─ 误差统计 ─────────────────────┐
│ ΔDx  RMSE          0.5907 m   │  ← _stat_row('ΔDx RMSE', '0.5907', 'm')
│ ΔDy  RMSE          0.3258 m   │
│ ΔDist RMSE         0.6746 m   │  ← 核心指标，加粗高亮
│ ΔDx  Mean          +0.2495 m  │
│ ΔDy  Mean          -0.2037 m  │
│ ΔDist Mean          0.5933 m  │
│ ─────────────────────────────  │  ← stats-separator
│ ΔVx  RMSE          0.1128 m/s │
│ ΔVy  RMSE          0.2538 m/s │
│ ΔV   RMSE          0.2778 m/s │
│ ─────────────────────────────  │
│ 匹配率           1150/1150    │
│ 匹配 ID                36     │
└────────────────────────────────┘
```

```python
# stats_panel.py 末尾追加

def render_cmp_error_stats(summary: dict, match_summary: dict) -> html.Div:
    """渲染误差统计卡片。（复用 _stat_row）"""
    rows = [
        _stat_row('ΔDx RMSE', _fmt_val(summary['pos_error_x']['rmse']), 'm'),
        _stat_row('ΔDy RMSE', _fmt_val(summary['pos_error_y']['rmse']), 'm'),
        _stat_row('ΔDist RMSE', _fmt_val(summary['pos_error_abs']['rmse']), 'm'),
        html.Hr(className='stats-separator'),
        _stat_row('ΔDx Mean', _fmt_val(summary['pos_error_x']['mean']), 'm'),
        _stat_row('ΔDy Mean', _fmt_val(summary['pos_error_y']['mean']), 'm'),
        html.Hr(className='stats-separator'),
        _stat_row('ΔVx RMSE', _fmt_val(summary['vel_error_x']['rmse']), 'm/s'),
        _stat_row('ΔVy RMSE', _fmt_val(summary['vel_error_y']['rmse']), 'm/s'),
        _stat_row('ΔV RMSE', _fmt_val(summary['vel_error_abs']['rmse']), 'm/s'),
        html.Hr(className='stats-separator'),
        _stat_row('匹配率', f'{match_summary["matched_frames"]}/{match_summary["total_frames"]}'),
    ]
    return html.Div([
        html.Div('误差统计', className='stats-card-title'),
        html.Div(rows, className='stats-compact-list'),
    ], className='stats-card')
```

### 7.2 分距离区间统计表

```
┌─ 分距离区间统计 ──────────────────┐
│  区间   │ 帧数 │ Mean │ RMSE │Max│
│ ────────────────────────────────── │
│ 0-10m   │ 200  │ 0.45 │ 0.52 │.98│
│ 10-20m  │ 350  │ 0.58 │ 0.65 │1.2│
│ 20-30m  │ 300  │ 0.62 │ 0.71 │1.5│
│ 30-40m  │ 180  │ 0.68 │ 0.78 │1.8│
│ 40-50m  │  80  │ 0.75 │ 0.85 │1.9│
│ 50-80m  │  30  │ 0.91 │ 0.98 │2.0│
│ 80-120m │  10  │ 1.12 │ 1.15 │1.9│
└────────────────────────────────────┘
```

```python
def render_cmp_distance_bins(bin_stats: list[dict]) -> html.Div:
    """渲染分距离区间统计表。"""
    rows = []
    for b in bin_stats:
        rows.append(html.Tr([
            html.Td(b['bin']),
            html.Td(str(b['frames'])),
            html.Td(f"{b['mean']:.3f}"),
            html.Td(f"{b['rmse']:.3f}"),
            html.Td(f"{b['max']:.3f}"),
        ]))
    
    return html.Div([
        html.Div('分距离区间统计', className='stats-card-title'),
        html.Table([
            html.Thead(html.Tr([
                html.Th('区间'), html.Th('帧数'),
                html.Th('Mean(m)'), html.Th('RMSE(m)'), html.Th('Max(m)'),
            ])),
            html.Tbody(rows),
        ], className='traj-table'),   # 复用 traj-table 样式
    ], className='stats-card')
```

---

## 八、图表设计

### 8.1 对比格式分类

| 对比指标 | 图表类型 | 数据源 |
|----------|----------|--------|
| `pos_error_x` / `pos_error_y` | **双线叠加**（雷达实线 + RTK虚线） + 误差散点 | aligned_df |
| `pos_error_abs` | **误差散点** + 零线 | aligned_df |
| `vel_error_x` / `vel_error_y` | **双线叠加**（雷达实线 + RTK虚线） + 误差散点 | aligned_df |
| `vel_error_abs` | **误差散点** + 零线 | aligned_df |
| `match_dist` | **匹配距离散点** + 5m阈值线 | aligned_df |

### 8.2 图示

```
子图1: ΔDx (纵向位置误差)
┌─────────────────────────────────┐
│  Dx (雷达蓝) ───  center_x (RTK绿) ---   │
│  · Dx雷达实线  ··· center_x RTK虚线       │
│  ΔDx = Dx - center_x_interp (右Y轴散点)  │
└─────────────────────────────────┘

子图2: ΔDy (横向位置误差)  
┌─────────────────────────────────┐
│  Dy (雷达) vs center_y (RTK)            │
│  (同上结构)                              │
└─────────────────────────────────┘

子图3: ΔDist (绝对位置误差)
┌─────────────────────────────────┐
│  ·   ·   ·  ·   ·  ·散点           │
│  ───────────────── 零线             │
│  ΔDist = √(ΔDx² + ΔDy²)            │
└─────────────────────────────────┘
```

---

## 九、回调链路设计

### 9.1 Store 组件（新增3个轻量状态）

| Store ID | 类型 | 用途 |
|----------|------|------|
| `cmp-files-loaded` | `bool` | 雷达+RTK两个文件是否都已上传 |
| `cmp-selected-id` | `int` | 用户选定的目标ID |
| `cmp-alignment-done` | `bool` | 对齐是否已完成 |

大数据（radar_df、rtk_df、aligned_df、summary）全部走 cache，不经过 Store。

### 9.2 回调清单

| # | 触发 | State | Output | 说明 |
|---|------|-------|--------|------|
| 1 | `mode-tab-wave.n_clicks` / `mode-tab-compare.n_clicks` | — | 面板显示/隐藏 + Tab样式 | 模式切换 |
| 2 | `cmp-upload-radar.contents` 或 `cmp-upload-rtk.contents` | 两文件 + 文件名 | 预览卡 + 反馈 + ID列表 + Store | 双文件上传→解析→预览→ID发现 |
| 3 | `cmp-id-list` 中点击某项 | `cmp-files-loaded` | 坐标诊断 + 延迟检测 + 选中ID Store | ID选定→自动诊断 |
| 4 | `cmp-run-btn.n_clicks` | `cmp-files-loaded`, `cmp-selected-id`, `cmp-delay-input.value`, `cmp-quantity-checklist.value` | 图表 + 统计 + 距离表 + 标题 + Store | 一键执行对齐 |
| 5 | `cmp-quantity-checklist.value` | `cmp-alignment-done`, `cmp-selected-id` | 图表更新 | 切换指标→重建子图 |
| 6 | `cmp-export-csv-btn.n_clicks` | `cmp-alignment-done` | 导出反馈 | 导出CSV |
| 7 | `cmp-export-img-btn.n_clicks` | `cmp-alignment-done` | 导出反馈 | 导出图表 |

### 9.3 关键回调伪代码

```python
# callbacks.py 末尾追加

@callback(
    Output('panel-wave', 'style'),
    Output('panel-compare', 'style'),
    Output('mode-tab-wave', 'className'),
    Output('mode-tab-compare', 'className'),
    Input('mode-tab-wave', 'n_clicks'),
    Input('mode-tab-compare', 'n_clicks'),
    prevent_initial_call=True,
)
def on_mode_switch(n_wave, n_compare):
    """Tab 切换：显示/隐藏对应面板"""
    ctx = callback_context
    if not ctx.triggered:
        raise PreventUpdate
    trig = ctx.triggered[0]['prop_id']
    if 'mode-tab-compare' in trig:
        return {'display': 'none'}, {'display': 'block'}, 'mode-tab', 'mode-tab active'
    return {'display': 'block'}, {'display': 'none'}, 'mode-tab active', 'mode-tab'


@callback(
    Output('cmp-preview-card', 'children'),
    Output('cmp-upload-feedback', 'children'),
    Output('cmp-id-list', 'children'),
    Output('cmp-files-loaded', 'data'),
    Input('cmp-upload-radar', 'contents'),
    Input('cmp-upload-rtk', 'contents'),
    State('cmp-upload-radar', 'filename'),
    State('cmp-upload-rtk', 'filename'),
    prevent_initial_call=True,
)
def on_cmp_upload(radar_content, rtk_content, radar_name, rtk_name):
    """双文件上传 → 解析 + 预览 + ID自动发现"""
    # 1. 解码 + 解析
    radar_info = load_csv_file(decode(radar_content), radar_name)
    rtk_info = load_csv_file(decode(rtk_content), rtk_name)
    
    # 2. 自动识别角色（允许swap）
    if radar_info['role'] != 'radar':
        radar_info, rtk_info = rtk_info, radar_info
    
    # 3. 校验
    errors = []
    if radar_info['role'] != 'radar': errors.append('雷达文件缺少Dx/Dy列')
    if rtk_info['role'] != 'rtk': errors.append('RTK文件缺少center_x/center_y列')
    if errors: return ..., ..., ..., False
    
    # 4. 缓存
    set_comparison_data('default', radar_info['df'], rtk_info['df'])
    
    # 5. ID自动发现（后台计算）
    id_list = discover_best_id(radar_info['df'], rtk_info['df'])
    
    # 6. 渲染预览卡 + ID列表
    preview = render_preview_card(radar_info, rtk_info)
    id_html = render_id_list(id_list)
    
    return preview, '', id_html, True


@callback(
    Output('cmp-graph', 'figure'),
    Output('cmp-graph-title', 'children'),
    Output('cmp-stats-content', 'children'),
    Output('cmp-bins-content', 'children'),
    Output('cmp-alignment-done', 'data'),
    Output('cmp-run-feedback', 'children'),
    Input('cmp-run-btn', 'n_clicks'),
    State('cmp-files-loaded', 'data'),
    State('cmp-selected-id', 'data'),
    State('cmp-delay-input', 'value'),
    State('cmp-quantity-checklist', 'value'),
    prevent_initial_call=True,
)
def on_cmp_run(_n, files_loaded, track_id, delay_ms, selected_qties):
    """一键执行对齐 + 展示全部结果"""
    # 1. 取缓存数据
    radar_df, rtk_df = get_comparison_data('default')
    
    # 2. 执行对齐
    result = align_trajectories(
        radar_df, rtk_df, track_id,
        delay_ms=delay_ms or 0,
    )
    
    # 3. 缓存结果
    set_alignment_result('default', result['aligned_df'], result['summary'])
    
    # 4. 构建图表 — 复用 build_comparison_subplots
    cmp_qties = get('comparison', {}).get('quantities', {})
    fig = build_comparison_subplots(
        result['aligned_df'], selected_qties, cmp_qties,
        trajectory_label=f'ID={track_id}'
    )
    
    # 5. 渲染统计 — 复用 _stat_row
    stats_html = render_cmp_error_stats(result['summary'], result['match_summary'])
    
    # 6. 渲染距离区间表
    bins = get('comparison', {}).get('distance_bins', [])
    bin_stats = compute_distance_bin_stats(result['aligned_df'], bins)
    bins_html = render_cmp_distance_bins(bin_stats)
    
    # 7. 标题
    title = [
        html.Span(f'ID={track_id}', className='traj-id-badge'),
        html.Span(' | ', style={'color':'#94a3b8'}),
        html.Span(f"{result['match_summary']['matched_frames']}/{result['match_summary']['total_frames']}帧匹配",
                  className='frame-count'),
    ]
    
    return fig, title, stats_html, bins_html, True, '对齐完成'
```

---

## 十、CSS 新增样式（追加到 custom.css 末尾）

```css
/* ============================================ */
/*  真值对比模式                                    */
/* ============================================ */

/* --- 模式切换 Tab --- */
.mode-tabs {
    background: #fff;
    border-bottom: 2px solid #e5e7eb;
    padding: 0 20px;
    display: flex;
    align-items: center;
    gap: 4px;
}
.mode-tab {
    padding: 8px 20px;
    border: none;
    background: none;
    font-size: 13px;
    font-weight: 500;
    color: #6b7280;
    cursor: pointer;
    border-bottom: 2px solid transparent;
    margin-bottom: -2px;
    transition: all 0.15s;
}
.mode-tab:hover { color: #374151; }
.mode-tab.active {
    color: #2563eb;
    border-bottom-color: #2563eb;
    font-weight: 600;
}

/* --- 对比图表 —— 复用 #trajectory-graph 的高度逻辑 --- */
#cmp-graph {
    width: 100% !important;
    height: calc(100vh - 150px) !important;
    min-height: 0;
}

/* --- 分距离区间表 —— 复用 traj-table + 微调 --- */
#cmp-bins-content table {
    width: 100%;
    font-size: 12px;
    border-collapse: collapse;
}
#cmp-bins-content th {
    text-align: center;
    padding: 5px 4px;
    color: #64748b;
    font-weight: 500;
    border-bottom: 1px solid #e2e8f0;
    background: #f8fafc;
    font-size: 11px;
}
#cmp-bins-content td {
    padding: 5px 4px;
    text-align: center;
    border-bottom: 1px solid #f1f5f9;
    color: #334155;
    font-size: 12px;
}
```

---

## 十一、缓存扩展（追加到 cache.py 末尾）

```python
# ============================================================
# 对比数据缓存（cmp: 前缀，与波动分析数据隔离）
# ============================================================

def set_comparison_data(key: str, radar_df, rtk_df):
    """缓存对比数据。"""
    cache.set(f'cmp:{key}:radar_df', radar_df)
    cache.set(f'cmp:{key}:rtk_df', rtk_df)

def get_comparison_data(key: str):
    """获取对比数据。"""
    return cache.get(f'cmp:{key}:radar_df'), cache.get(f'cmp:{key}:rtk_df')

def set_alignment_result(key: str, aligned_df, summary):
    """缓存对齐结果。"""
    cache.set(f'cmp:{key}:aligned_df', aligned_df)
    cache.set(f'cmp:{key}:summary', summary)

def get_alignment_result(key: str):
    """获取对齐结果。"""
    return cache.get(f'cmp:{key}:aligned_df'), cache.get(f'cmp:{key}:summary')

def clear_comparison_data(key: str = 'default'):
    """清除对比数据。"""
    for suffix in ['radar_df', 'rtk_df', 'aligned_df', 'summary']:
        cache.delete(f'cmp:{key}:{suffix}')
```

---

## 十二、实现阶段划分

| Phase | 内容 | 文件 | 行数 |
|-------|------|------|------|
| **1** | `comparison/` 骨架 + `parser.py` + `config.yaml` + `cache.py` 扩展 | 4文件 | ~200 |
| **2** | `id_finder.py` + `coord_diag.py` + `delay_detect.py` | 3文件 | ~300 |
| **3** | `alignment.py`（核心对齐算法） | 1文件 | ~250 |
| **4** | `exporter.py` + `stats_panel.py` 追加渲染函数 | 2文件 | ~120 |
| **5** | `comparison_charts.py`（图表构建） | 1文件 | ~180 |
| **6** | `layout.py` 追加模式切换 + 对比面板 + `custom.css` | 2文件 | ~200 |
| **7** | `callbacks.py` 追加7个对比回调 | 1文件 | ~500 |
| **8** | 联调测试 + 打包验证 | — | — |

每个 Phase 完成后可独立打包验证。

---

## 十三、总结

| 设计要点 | 说明 |
|----------|------|
| **布局完全对标** | 左侧面板→中间(指标多选+图表)→右侧(统计+间距表+导出)，三栏2-7-3比例分布 |
| **组件大量复用** | `_stat_row`、`app-card`、`stats-card`、`qty-checklist`、`export-btn` 等直接复用 |
| **config 结构兼容** | `comparison.quantities` 格式与 `quantities` 一致，Checklist 构建逻辑可复用 |
| **cache 模式一致** | `cmp:*` 前缀隔离，大数据走 cache，轻量状态走 Store |
| **回调链模式一致** | `prevent_initial_call=True`、`allow_duplicate`、Pattern-Matching Callback |
| **零影响已有功能** | 默认显示波动分析面板，对比面板 `display:none`；Tab 切换激活 |
