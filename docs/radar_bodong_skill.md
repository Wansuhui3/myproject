# 雷达目标轨迹波动分析系统 需求说明书（V2.1）

> **V2.0 → V2.1 修订摘要**：新增雷达位置选择功能（前角/后角），新增绝对时间戳精确定位筛选，新增物理量曲线图自定义选择。交互式筛选模块全面增强。
>
> V1.0 → V2.0 修正摘要：基于实际路试数据验证，修正了 Track_Age 分段核心规则（原规则将回绕误判为新轨迹、遗漏 ID 复用），新增 uint8 回绕展开处理。其余模块（波动计算、交互筛选、框选分析、导出）逻辑不变。

---

## 一、项目背景与总体目标

### 1.1 背景

雷达多目标跟踪数据中，目标 ID 存在回收复用机制，同一 ID 号可在不同时间段对应不同真实目标，无法直接按 ID 分组统计。此外，Track_Age 字段为 uint8 计数器（0~255），存在回绕溢出，首帧 Track_Age 不一定为 1。

### 1.2 总体目标

1. 基于 Track_Age 单调性与回绕检测完成精准轨迹分段，将原始数据拆分为独立连续的单目标轨迹
2. 基于帧间差分计算波动指标，直接反映测量值的瞬时抖动程度
3. 提供交互式筛选能力，支持按时间段定位目标 ID 并查看对应轨迹
4. 支持曲线局部框选分析，可对任意区间单独计算波动指标
5. **（V2.1 新增）** 支持雷达位置选择（前角 / 后角），可手动切换不同雷达数据源进行分析
6. **（V2.1 新增）** 支持输入绝对时间戳精确定位，自动筛选该时刻出现的目标并支持选择具体目标与所需曲线图

---

## 二、数据预处理与轨迹分段规则

### 2.1 输入数据

原始雷达跟踪 CSV 数据，必含字段：

| 字段 | 说明 | 数据特征 |
|------|------|----------|
| timestamp | 时间戳 | 格式 `YYYY_MM_DD_HH_MM_SS_mmm`，标称采样周期 50ms（20Hz） |
| ID | 目标编号 | 整数，存在回收复用机制 |
| Track_Age | 跟踪年龄 | **uint8 计数器，范围 0~255**，目标首次检测时从 0 或 1 开始递增，到达上限后回绕至 0 继续递增 |
| Dx, Dy | 目标位置 | 纵向/横向距离（m） |
| Vx, Vy | 目标速度 | 纵向/横向速度（m/s） |
| 其他物理量 | Ax, Ay, HeadingAngle 等 | 按需分析 |

### 2.2 预处理规则

1. **数据清洗**：剔除空值、NaN、物理量超量程的无效行
2. **时间戳统一**：解析为可比较的数值格式，全局按时间戳升序排列
3. **按目标 ID 初步分组**：组内保持时间升序
4. **Track_Age 类型校验**：确认 Track_Age 列为整数类型，值域 0~255；超出此范围的行标记为异常

### 2.3 轨迹分段核心规则（V2.0 修正版）

> **原 V1.0 规则"Track_Age == 1 为新轨迹起点"已被推翻。** 实际数据中：首帧 Track_Age 可为任意值（目标在录制前已被跟踪）；255→0→1 是计数器溢出回绕而非新轨迹；同一 ID 内 Track_Age 非回绕下降才是 ID 复用标志。

分段判据为以下四条规则，按优先级依次判定：

#### 规则 A — ID 首现即起点

某 ID 在数据中的**首次出现**，无论 Track_Age 值是多少，即为该轨迹段在当前数据集内的起点。

- 例：首帧 Track_Age = 14，表示目标在录制开始前已被跟踪 14 个周期，仍作为合法轨迹起点
- 例：首帧 Track_Age = 58，同上

#### 规则 B — uint8 回绕 = 延续，不切分

同一 ID 分组内，Track_Age 从接近上限值（≥250）降至低值（≤5）时，判定为**计数器溢出回绕**，目标仍在连续跟踪中，不做切分。

- 判定条件：`Track_Age[i-1] ≥ 250` 且 `Track_Age[i] ≤ 5` 且 `Track_Age[i-1] > Track_Age[i]`
- 例：255→0→1→2… 是回绕，同一轨迹延续
- 例：254→0 也是回绕（计数器未严格执行 255→0 的精确跳变）

#### 规则 C — 非回绕下降 = ID 复用，切分新段

同一 ID 分组内，Track_Age 出现下降且**不符合回绕条件**（即上一帧 Track_Age < 250），判定为 ID 号被雷达回收后重新分配给另一个目标，在此下降点切分为新轨迹段。

- 判定条件：`Track_Age[i] < Track_Age[i-1]` 且 `Track_Age[i-1] < 250`
- 例：Track_Age 从 168→8，该 ID 被复用，切分
- 例：Track_Age 从 83→14，该 ID 被复用，切分
- 例：Track_Age 从 103→12，该 ID 被复用，切分

#### 规则 D — 时间间隔过大 = ID 消失后重现，切分新段

同一 ID 分组内，相邻帧时间差超过阈值时，判定为目标曾消失后重新出现，**无条件切分为新段**。无需检查位置跳变——ID 消失重现本身即说明雷达丢失并重新捕获目标，无论重现位置是否接近，都应视为独立轨迹段。

- 时间间隔阈值：N 倍标称采样周期（默认 N=10，即 500ms，可配置）
- 判定逻辑：`time_diff > time_threshold` → 切分新段
- **不设位置跳变辅助判据**：即使重现位置与消失前接近，也切分。理由：目标消失期间的运动状态未知，跨缺失区间做帧间差分无物理意义，强行连续会导致波动计算失真

### 2.4 Track_Age 展开处理

对分段后的每段轨迹，执行 uint8 展开（unwrap），使 Track_Age 序列变为单调递增的等效值，便于元信息统计和辅助校验：

- 检测回绕点（规则 B 标识的位置），对回绕后的帧累加 256 偏移量
- 展开算法：遍历 Track_Age 序列，当检测到回绕时 `offset += 256`，后续帧 `unwrapped_age = raw_age + offset`
- 展开后的 Track_Age 仅用于辅助判断和元信息统计（如计算等效最大跟踪年龄），**不参与波动计算**（波动仍基于原始物理量帧间差分）

示例：

```
原始序列:   14, 14, 15, ..., 255, 0, 1, 2, ..., 185
展开序列:   14, 14, 15, ..., 255, 256, 257, 258, ..., 441
```

### 2.5 辅助校验规则（增强鲁棒性）

在上述核心规则之外，增加以下校验：

1. **展开后 Track_Age 非单调**：同段内展开后 Track_Age 出现下降（排除 diff=0 的重复帧），标记该段为异常轨迹，单独列出供排查
2. **Track_Age 跳变过大**：同段内展开后 Track_Age 递增超过 2（排除重复帧），可能是帧丢失，记录但不切分
3. **位置连续性校验**：同段内相邻帧位移超过物理合理阈值（如 50ms 内位移 > 10m）时，标记为可疑断点，由人工确认是否切分

### 2.6 轨迹元信息索引

构建轨迹元信息表，每条轨迹对应一行，字段包含：

| 字段 | 说明 |
|------|------|
| trajectory_id | 全局唯一标识，命名规则 `{原始ID}_seg{序号}` |
| original_id | 原始目标 ID |
| start_time | 轨迹起始时间戳 |
| end_time | 轨迹结束时间戳 |
| total_frames | 总帧数 |
| first_track_age | 首帧原始 Track_Age |
| max_raw_age | 原始 Track_Age 最大值 |
| max_unwrapped_age | 展开后等效最大 Track_Age |
| num_wraps | 回绕次数 |
| is_abnormal | 是否被标记为异常轨迹 |

作用：用于后续快速筛选查询，无需加载全量明细数据。

---

## 三、波动计算规则

### 3.1 波动定义

采用相邻帧差分作为波动计算口径，不做趋势拟合：

> 第 i 帧波动值 = 第 i 帧数值 − 第 i−1 帧数值

- 每段轨迹首帧无波动值
- 波动单位与原物理量保持一致
- **波动基于原始物理量计算，不基于 Track_Age**

### 3.2 统计指标集

对指定区间内的波动值计算以下指标：

| 指标 | 计算方式 | 说明 |
|------|----------|------|
| 有效样本数 | count(diff) | 参与计算的波动值数量 |
| 波动绝对值平均值 | mean(abs(diff)) | 平均抖动幅度 |
| 波动标准差 | std(diff) | 整体抖动离散程度 |
| 波动均方根 RMS | sqrt(mean(diff²)) | 等效抖动能量 |
| 波动峰峰值 | max(diff) + abs(min(diff)) | 最大正向跳变与最大负向跳变绝对值之和 |
| 最大正向跳变 | max(diff) | 单帧最大上升幅度 |
| 最大负向跳变 | min(diff) | 单帧最大下降幅度 |

> 注意：峰峰值定义口径为 `max_jump + |min_jump|`，与传统 `max - min` 不同，文档及 UI 中需注明。

### 3.3 无效轨迹过滤

总帧数 < 设定阈值（默认 20 帧）的轨迹段，不参与波动统计，标记为「样本不足」。

---

## 四、交互式筛选功能

### 4.0 雷达位置选择（前角 / 后角）（V2.1 新增）

#### 4.0.1 功能说明

系统启动后，界面顶部提供**雷达位置选择控件**，用户可手动选择当前分析的数据来源：

| 选项 | 说明 | 典型数据源 |
|------|------|-----------|
| **前角雷达** | 安装于车辆前部两侧的角雷达 | 前左角、前右角雷达数据 |
| **后角雷达** | 安装于车辆后部两侧的角雷达 | 后左角、后右角雷达数据 |

#### 4.0.2 交互逻辑

1. **数据源绑定**：每个雷达位置选项对应一组 CSV 数据文件路径（或同一文件中的数据子集）。用户选择雷达位置后，系统自动加载对应数据并重新执行预处理 → 分段 → 波动计算全流程
2. **状态联动**：切换雷达位置时，清空当前所有筛选状态（时间范围、ID 选择、轨迹段选择、曲线图），恢复初始状态
3. **界面标识**：界面显著位置持续显示当前雷达位置标签（如 `[前角雷达]`），避免用户混淆数据来源
4. **多文件支持**：若同一雷达位置有多个 CSV 文件（如前左角 + 前右角），提供二级选择控件进一步指定具体文件；也可选择「全部」合并加载

#### 4.0.3 数据配置

雷达位置与数据文件的映射关系通过配置文件管理，格式示例：

```json
{
  "radar_sources": {
    "front_corner": {
      "label": "前角雷达",
      "files": [
        {"name": "前左角", "path": "E:/data/front_left/*.csv"},
        {"name": "前右角", "path": "E:/data/front_right/*.csv"}
      ]
    },
    "rear_corner": {
      "label": "后角雷达",
      "files": [
        {"name": "后左角", "path": "E:/data/rear_left/*.csv"},
        {"name": "后右角", "path": "E:/data/rear_right/*.csv"}
      ]
    }
  }
}
```

> 说明：实际项目中雷达数据文件命名通常已包含位置信息（如文件名含 `flr`/`rlr` 等），系统可自动识别并预填充配置，用户也可手动修改。

### 4.1 时间段筛选

#### 4.1.1 绝对时间戳精确定位（V2.1 新增）

支持用户直接输入**绝对时间戳**来定位目标，是日常分析最高频的使用方式：

- **输入控件**：提供日期时间输入框，格式 `YYYY-MM-DD HH:MM:SS`（如 `2026-07-03 11:56:16`）
- **时间窗口**：以输入时间戳为中心，自动取前后各 N 秒（默认 ±30 秒，可配置）作为筛选窗口
- **快捷输入**：支持从剪贴板粘贴时间戳，自动识别常见格式（含/不含毫秒、含/不含日期分隔符等）
- **联动效果**：输入时间戳后，系统自动执行以下流程：
  1. 解析时间戳，在当前雷达数据中筛选该时间窗口内的所有轨迹段
  2. 自动提取窗口内出现的所有目标 ID，填充到 4.2 节的 ID 选择列表
  3. ID 列表按首次出现时间排序，每个 ID 标注与输入时间戳的时间差（如 `ID: 8 (距输入时刻 +2.3s 首次出现)`），帮助用户快速定位
  4. 自动选中距输入时间戳最近的轨迹段，绘制曲线图

#### 4.1.2 时间范围筛选（原有功能，保留）

- 提供起止时间选择控件，支持自定义输入时间范围
- 提供快捷选项：全量数据、前 1 分钟、前 5 分钟等
- 筛选逻辑：返回所有与选定时间段有时间交集的轨迹段
- **（V2.1 增强）** 起止时间控件也支持绝对时间戳输入，与 4.1.1 的精确定位模式互补

#### 4.1.3 两种模式的关系

| 模式 | 适用场景 | 输入方式 | 筛选窗口 |
|------|---------|---------|---------|
| 精确定位 | 已知异常发生时刻，快速定位 | 单个时间戳 | ±N 秒自动窗口 |
| 范围筛选 | 分析一段时间段内的整体情况 | 起止时间 | 用户自定义区间 |

两种模式可切换使用，切换时清空下游选择状态。

### 4.2 目标 ID 选择

- 根据选定时间段，自动提取该时段内出现过的所有目标 ID（去重），以下拉列表形式展示
- 每个 ID 后标注该时段内包含的轨迹段数量与总帧数（如 `ID: 8 (3段, 355帧)`）
- **（V2.1 新增）** 精确定位模式下，每个 ID 额外标注距输入时间戳的时间差，按距离排序
- **（V2.1 新增）** 支持搜索过滤：ID 数量多时，可在下拉框中输入数字快速过滤
- 用户选定 ID 后，列出该 ID 在选定时间段内的所有轨迹段清单

### 4.3 轨迹段列表展示

以表格形式展示轨迹段信息，列包含：

trajectory_id、起始时间、结束时间、总帧数、首帧 Track_Age、回绕次数、全段波动标准差

支持按指标列排序，快速定位波动最大的轨迹段。

**（V2.1 新增）** 精确定位模式下，表格高亮显示与输入时间戳最接近的轨迹段行，并在该行标注 `← 最近匹配`。

### 4.4 物理量曲线图选择（V2.1 新增）

#### 4.4.1 功能说明

选定轨迹段后，用户可选择需要查看的物理量曲线，系统据此绘制对应的原始数据曲线图：

| 可选物理量 | 字段名 | 单位 | 说明 |
|-----------|--------|------|------|
| 纵向距离 | Dx | m | 目标与雷达的纵向距离 |
| 横向距离 | Dy | m | 目标与雷达的横向距离 |
| 纵向速度 | Vx | m/s | 目标纵向相对速度 |
| 横向速度 | Vy | m/s | 目标横向相对速度 |
| 纵向加速度 | Ax | m/s² | 目标纵向加速度 |
| 横向加速度 | Ay | m/s² | 目标横向加速度 |
| 航向角 | HeadingAngle | ° | 目标航向角 |
| 合成速度 | Vabs | m/s | 目标合成速度 |

#### 4.4.2 交互逻辑

1. **单选模式（默认）**：选择一个物理量，曲线图 Y 轴对应该物理量，波动统计也基于该物理量计算
2. **多选叠加模式**：可选择 2~4 个物理量叠加显示在同一曲线图上，各曲线用不同颜色区分，Y 轴自适应或使用双 Y 轴（左右各一）
3. **快速切换**：曲线图上方提供物理量切换标签栏（Tab 或 Chip 按钮组），一键切换不同物理量，无需回到上一步重新选择
4. **状态记忆**：切换轨迹段时，保持当前选中的物理量不变，便于横向对比不同轨迹的同一物理量

#### 4.4.3 波动统计与物理量的关系

- 切换物理量后，波动统计指标（均值、标准差、RMS 等）自动基于新选中的物理量重新计算
- 多选叠加模式下，波动统计默认针对主曲线（第一个选中的物理量）计算，也可在统计面板中切换查看各物理量的波动指标

#### 4.4.4 曲线图默认行为

- 首次进入轨迹曲线图页面时，默认展示 **Dx（纵向距离）** 曲线
- 精确定位模式下，自动将曲线图视图窗口对齐到输入时间戳附近（±10 秒），用户可手动缩放查看全局

---

## 五、局部框选分析功能（核心交互）

### 5.1 原始曲线展示

- 选中单段轨迹后，绘制该轨迹的原始数据曲线图（仅原始曲线，不叠加拟合线）
- **（V2.1 更新）** 曲线 Y 轴数据由 4.4 节物理量选择决定，用户可随时切换物理量
- 默认展示全段轨迹的波动统计指标
- **（V2.1 更新）** 精确定位模式下，自动定位到输入时间戳附近区间并高亮

### 5.2 框选交互

- 支持在曲线图上沿时间轴（横轴）拖拽鼠标框选任意局部区间
- 框选后自动获取区间的起止时间，筛选对应区间内的原始数据
- 支持点击空白处取消框选，恢复全段统计

### 5.3 局部统计

- 框选完成后，实时计算选中区间内的帧间波动及全部统计指标
- 界面上独立展示「选中区域统计结果」，与全段结果区分显示
- 选中区域在曲线上自动高亮标识

---

## 六、异常与边界处理规则

### 6.1 Track_Age 相关异常

| 异常场景 | 处理方式 |
|----------|----------|
| Track_Age 值超出 0~255 | 标记为异常行，不参与分段与统计 |
| Track_Age 为非整数 | 标记为异常行 |
| Track_Age 出现非回绕下降 | 按规则 C 切分为新段（ID 复用） |
| Track_Age 回绕点后出现异常跳变 | 展开后检查单调性，异常段单独标记 |
| 首帧 Track_Age 为任意值 | 按规则 A 正常处理，不视为异常 |

### 6.2 框选与数据异常

| 异常场景 | 处理方式 |
|----------|----------|
| 框选区间样本不足（< 2 帧） | 提示「选中区域数据过少，无法计算波动」 |
| 框选区间包含数据缺失标记 | 按缺失点拆分为多段分别计算，或提示区间存在数据中断 |
| 框选跨轨迹断点 | 检测框选区间内的 ID 复用断点，自动拆分计算 |
| 时间段无数据 | 提示「该时段无有效目标」 |
| ID 无对应数据 | 清空列表并提示重新选择 |

### 6.2.1 雷达位置与时间戳相关异常（V2.1 新增）

| 异常场景 | 处理方式 |
|----------|----------|
| 雷达位置选择后数据文件不存在 | 提示「未找到 {前角/后角} 雷达数据文件，请检查配置路径」 |
| 雷达位置配置文件缺失 | 使用默认配置（单文件模式），提示「未找到雷达位置配置，已加载当前文件」 |
| 输入时间戳格式无法解析 | 提示「时间格式不正确，请使用 YYYY-MM-DD HH:MM:SS 格式」 |
| 输入时间戳超出数据时间范围 | 提示「输入时刻 {timestamp} 超出数据范围（{最早} ~ {最晚}）」并自动定位到最近的数据边界 |
| 输入时间戳窗口内无目标 | 提示「该时刻 ±{N} 秒内无有效目标」，建议扩大时间窗口或检查雷达位置选择 |
| 输入时间戳窗口内仅有样本不足的轨迹 | 列出轨迹但标注「样本不足」，不自动选中绘制曲线 |
| 切换雷达位置时仍有未保存的分析结果 | 弹出确认提示「切换雷达将清空当前分析，是否继续？」 |

### 6.3 大数据量处理

| 场景 | 处理方式 |
|------|----------|
| 单轨迹帧数过高（> 2000） | 显示层自动降采样渲染（保留关键转折点），统计计算仍使用原始全量数据 |
| 全量数据行数过大（> 50000） | 预构建时间索引（按分钟分桶），加速时间段筛选 |
| 单 CSV 文件过大（> 200MB） | 支持分块加载，仅加载当前分析时段的数据 |

### 6.4 段内帧间间隔异常处理

规则 D 已将所有时间间隔超阈值的断点切分为新段。但同一段轨迹内仍可能存在**小于阈值但大于标称采样周期**的帧间隔（如采样周期 50ms，阈值 500ms，某处间隔 200ms 表示丢了 3 帧但未达到切分条件）：

- 在曲线图上对该类间隔处用虚线连接，直观标识帧丢失
- 波动计算时，帧间隔大于 2 倍标称采样周期的相邻帧**不做差分**（跳过该处 diff），避免将帧丢失造成的位置突变误计为波动
- 元信息中记录段内异常间隔数量和总时长

---

## 七、输出物要求

> **（V2.1 新增）** 所有导出文件名和内容中均包含当前雷达位置标识（如 `front_corner` / `rear_corner`），便于区分数据来源。

### 7.1 轨迹元信息索引表

CSV 格式，包含 2.6 节定义的全部字段，每条轨迹一行。**（V2.1 新增）** 首列增加 `radar_position` 字段标识雷达位置。

### 7.2 单轨迹原始数据与波动数据

CSV 格式导出，列包含：时间戳、原始物理量、Track_Age、展开后 Track_Age、帧间波动值。支持仅导出选中区间。**（V2.1 新增）** 导出当前选中的全部物理量列。

### 7.3 轨迹曲线图

支持导出为 PNG/SVG 图片。**（V2.1 新增）** 图片标题包含雷达位置、轨迹 ID、物理量名称。

### 7.4 波动统计结果汇总表

CSV 格式，每条轨迹一行，包含所有统计指标。**（V2.1 新增）** 包含 `radar_position` 和 `quantity`（物理量名称）列。

---

## 八、技术实现参考

### 8.1 推荐技术栈

| 层 | 选型 | 理由 |
|----|------|------|
| 数据处理 | pandas + numpy | CSV 处理能力强，支持向量化运算 |
| 可视化 | Plotly | 原生支持 boxselect 事件，交互能力强 |
| Web 框架 | Dash | 与 Plotly 无缝集成，回调机制适配联动筛选 |
| 降采样 | plotly-resampler | Plotly 官方降采样插件，自动处理大数据渲染 |
| 图片导出 | kaleido | Plotly 官方图片导出引擎 |
| **雷达位置选择**（V2.1） | dcc.RadioItems / dcc.Dropdown | Dash 原生组件，支持前角/后角切换 |
| **时间戳输入**（V2.1） | dcc.Input + 客户端校验 | 支持文本输入 + 自动解析多种时间格式 |
| **物理量选择**（V2.1） | dcc.Checklist / dbc.ButtonGroup | 支持单选/多选叠加模式 |

### 8.2 分段算法伪代码

```python
def segment_by_id(id_group_df):
    ages = id_group_df['Track_Age'].values
    dx = id_group_df['Dx'].values
    dy = id_group_df['Dy'].values
    ts = id_group_df['timestamp'].values

    breakpoints = []
    offset = 0
    unwrapped = ages.copy()

    for i in range(1, len(ages)):
        diff = ages[i] - ages[i-1]

        # 规则 B: uint8 回绕 → 延续
        if ages[i-1] >= 250 and ages[i] <= 5 and diff < 0:
            offset += 256
            unwrapped[i] = ages[i] + offset
            continue

        # 规则 C: 非回绕下降 → ID 复用, 切分
        if diff < 0:
            breakpoints.append(i)
            offset = 0  # 新段重置偏移
            unwrapped[i] = ages[i]
            continue

        # 正常递增或重复帧
        unwrapped[i] = ages[i] + offset

        # 规则 D: 时间间隔过大 → ID 消失后重现, 无条件切分
        time_diff = parse_ts(ts[i]) - parse_ts(ts[i-1])
        if time_diff > GAP_THRESHOLD:
            breakpoints.append(i)
            offset = 0
            unwrapped[i] = ages[i]

    return breakpoints, unwrapped
```

### 8.3 关键配置参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| SAMPLING_PERIOD_MS | 50 | 标称采样周期（ms） |
| GAP_THRESHOLD | 500 | 时间间隔切分阈值（ms），= 10 × SAMPLING_PERIOD_MS |
| MIN_TRAJ_FRAMES | 20 | 有效轨迹最小帧数 |
| WRAP_HIGH | 250 | 回绕判定上限 |
| WRAP_LOW | 5 | 回绕判定下限 |
| TIMESTAMP_WINDOW_SEC | 30 | 精确定位模式的时间窗口半径（秒），输入时间戳前后各取 N 秒 |
| DEFAULT_QUANTITY | Dx | 默认展示的物理量曲线 |
| MAX_OVERLAY_CURVES | 4 | 多选叠加模式最大同时显示曲线数 |

所有参数均为可配置，不同雷达型号/场景可调整。

### 8.4 界面交互流程（V2.1 新增）

```
用户启动系统
    │
    ▼
选择雷达位置（前角 / 后角）──→ 加载对应数据 → 预处理 → 分段 → 波动计算
    │
    ▼
输入时间戳或选择时间范围 ──→ 筛选时间窗口内轨迹段
    │                         │
    │                         ▼
    │                    提取目标 ID 列表（标注距输入时刻时间差）
    │                         │
    │                         ▼
    │                    选择目标 ID ──→ 列出该 ID 的轨迹段
    │                                      │
    │                                      ▼
    │                                 选择轨迹段 ──→ 绘制原始曲线
    │                                                │
    │                                                ▼
    │                                           选择物理量（Dx/Dy/Vx/Vy…）
    │                                                │
    │                                    ┌───────────┴───────────┐
    │                                    ▼                       ▼
    │                              查看全段统计            框选局部区间
    │                                    │                       │
    │                                    │                       ▼
    │                                    │              局部统计 + 高亮
    │                                    │                       │
    │                                    └───────────┬───────────┘
    │                                                ▼
    └─────────────────────────────────────── 导出结果（CSV / 图片）
```

---

## 附录 A：修订记录

### V2.0 → V2.1 修订

| 章节 | 修订内容 | 修订原因 |
|------|----------|----------|
| 1.2 | 新增目标 5（雷达位置选择）、目标 6（时间戳定位 + 曲线选择） | 用户需求：需手动切换前角/后角雷达，按绝对时间戳定位目标 |
| 4.0 | **新增** 雷达位置选择（前角/后角）完整功能定义 | 不同角雷达数据需分别分析，界面需支持手动切换 |
| 4.1 | **重写** 时间段筛选，新增绝对时间戳精确定位模式 | 用户需输入具体时刻（如 2026-07-03 11:56:16）快速定位目标 |
| 4.2 | ID 选择列表增加时间差标注和搜索过滤 | 精确定位模式下需快速识别距输入时刻最近的目标 |
| 4.3 | 轨迹段表格增加最近匹配高亮 | 提升定位效率 |
| 4.4 | **新增** 物理量曲线图选择完整功能定义 | 用户需自选 Dx/Dy/Vx/Vy 等不同曲线分析 |
| 5.1 | 曲线展示与 4.4 物理量选择衔接 | 曲线 Y 轴由用户选择决定 |
| 6.2.1 | **新增** 雷达位置与时间戳相关异常处理 | 新功能引入的边界场景需覆盖 |
| 8.3 | 新增 TIMESTAMP_WINDOW_SEC、DEFAULT_QUANTITY、MAX_OVERLAY_CURVES 参数 | 新功能可配置项 |
| 8.4 | **新增** 界面交互流程图 | 完整展示从启动到导出的用户操作路径 |

### V1.0 → V2.0 修订

| 章节 | 修订内容 | 修订原因 |
|------|----------|----------|
| 2.1 | 新增 Track_Age 数据特征说明（uint8, 0~255） | 实际数据验证发现 Track_Age 为 uint8 计数器 |
| 2.3 | **全文重写**：四条规则替代原"Track_Age==1分段"规则 | 原规则将回绕误判为新轨迹、遗漏 ID 复用，7140 行路试数据验证确证错误 |
| 2.4 | 新增 Track_Age 展开处理章节 | 回绕后需等效展开才能正确统计元信息 |
| 2.5 | 辅助校验规则更新：展开后单调性检查、Track_Age 跳变记录 | 适配 uint8 展开后的校验需求 |
| 2.6 | 元信息索引增加 max_unwrapped_age、num_wraps、is_abnormal 字段 | 回绕和异常标记需要持久化 |
| 3.2 | 峰峰值定义注明口径为 max+|min| | 避免与传统 max-min 定义混淆 |
| 4.2 | ID 下拉列表增加帧数标注 | 帮助用户快速判断数据量 |
| 6.1 | 新增 Track_Age 相关异常处理表 | uint8 回绕和 ID 复用引入新的异常场景 |
| 6.3 | 大数据量阈值和分块加载策略 | 7140+ 行路试数据需要性能优化 |
| 6.4 | 改为段内帧间间隔异常处理（原"数据缺失区间"已废弃） | 规则 D 不再保留不切分的数据缺失，段内小间隔需独立处理 |
| 8.x | 新增技术实现参考章节 | 提供伪代码和配置参数，降低实现歧义 |

### V2.1 内部修正记录

| 章节 | 修订内容 | 修订原因 |
|------|----------|----------|
| 2.3 规则 D | 移除位置跳变辅助判据，改为时间间隔超阈值即无条件切分 | ID 消失重现期间运动状态未知，即使位置接近也不应跨缺失区间做差分，否则波动计算失真 |
| 6.4 | 原数据缺失区间处理改为段内帧间间隔异常处理 | 规则 D 不再产生"不切分的数据缺失" |
| 8.2 伪代码 | 移除 pos_jump 变量和条件判断 | 适配规则 D 简化 |
| 8.3 配置参数 | 移除 POS_THRESHOLD 参数 | 不再需要位置跳变阈值 |

## 附录 B：验证数据摘要

| 数据集 | 行数 | ID 数 | 修正算法段数 | 原规则段数 | 差异 ID 数 |
|--------|------|-------|-------------|-----------|-----------|
| case19 (单 ID) | 599 | 1 | 1 | 3 | 1 |
| 20260623 路试 (多 ID) | 7140 | 40 | 69 | 53 | 20 |

关键验证案例：
- ID=9（1次回绕）：修正=1段，原规则=2段 → 回绕不误切 ✅
- ID=8（2次ID复用）：修正=3段，原规则=1段 → 复用不遗漏 ✅
- ID=64（3个物理目标）：修正=3段，原规则=1段 → Dy跳变62m正确识别 ✅
- ID=55（首帧Age=13）：修正正常识别，原规则无Age==1标记则遗漏 ✅
