# 雷达目标轨迹波动分析系统 — 开发规则

> 本文档是项目开发的强制性约束。所有代码提交前须对照本文件自查。

---

## 一、项目结构

```
radar_wave_analyzer/
├── app.py                    # Dash 应用入口 + 布局定义
├── callbacks.py              # 回调函数集合
├── config.py                 # 配置参数（从 config.yaml 加载，提供默认值）
├── core/                     # 业务逻辑层（纯 Python，零 UI 依赖）
│   ├── data_loader.py        # CSV 加载 + 时间戳解析 + 预处理
│   ├── segmenter.py          # 四层分段算法 + Track_Age 展开
│   ├── wave_calc.py          # 帧间差分 + 统计指标计算
│   └── exporter.py           # CSV / 图片导出
├── components/               # UI 组件层
│   ├── layout.py             # 三栏布局
│   ├── stats_panel.py        # 统计面板渲染
│   └── graph_builder.py      # Plotly 图表构建
├── tests/                    # 单元测试
│   ├── test_segmenter.py
│   ├── test_wave_calc.py
│   └── test_data_loader.py
├── config.yaml               # 运行时配置（阈值、路径、雷达映射）
└── requirements.txt
```

### 规则

1. **`core/` 目录禁止 import 任何 Dash / Plotly 模块。** 这一层必须是纯 Python，可以被 CLI 脚本、测试、其他项目直接引用
2. **`components/` 目录禁止包含业务计算逻辑。** 只负责把 `core/` 的输出渲染成 UI
3. **`callbacks.py` 是唯一连接 `core/` 和 `components/` 的桥梁。** 回调函数调用 `core/` 获取数据，传给 `components/` 渲染
4. **配置全部走 `config.yaml`，代码中不硬编码阈值。** `config.py` 负责加载并提供默认值兜底

---

## 二、核心算法规则

### 2.1 轨迹分段 — 四层判断（不可变更）

分段算法按规则 A → B → C → D 优先级依次判定，命中即处理，不回溯：

| 规则 | 判据 | 动作 | 禁止事项 |
|------|------|------|----------|
| **A** ID 首现 | ID 在数据中首次出现 | 开启新段，无论 Track_Age 值 | 禁止要求首帧 Track_Age == 1 |
| **B** uint8 回绕 | `age[i-1] >= 250` 且 `age[i] <= 5` 且 `diff < 0` | 同段延续，offset += 256 | 禁止在此处切分 |
| **C** 非回绕下降 | `diff < 0` 且不满足规则 B 条件 | 切分新段 | 禁止忽略任何非回绕下降 |
| **D** 时间间隔 | `time_diff > GAP_THRESHOLD` | **无条件切分新段** | **禁止添加位置跳变辅助判据** |

**铁律：**
- 规则 D 不检查位置跳变。ID 消失重现期间运动状态未知，即使重现位置接近也必须切分
- 四条规则的判定顺序不可调换。先检查回绕（B），再检查非回绕下降（C），最后检查时间间隔（D）
- 重复帧（`diff == 0`）不触发任何规则，正常归入当前段

### 2.2 Track_Age 展开处理

- 回绕点处 `offset += 256`，后续帧 `unwrapped = raw_age + offset`
- 展开后的 Track_Age 仅用于元信息统计和辅助校验
- **展开后的 Track_Age 不参与波动计算**，波动始终基于原始物理量帧间差分

### 2.3 波动计算

- 波动 = 帧间差分：`wave[i] = value[i] - value[i-1]`
- 每段轨迹首帧无波动值（NaN 或跳过）
- 段内帧间隔 > 2 倍标称采样周期时，该处不做差分（跳过）
- **禁止跨段做差分**——不同轨迹段之间不计算波动

### 2.4 统计指标口径

| 指标 | 公式 | 注意 |
|------|------|------|
| 波动绝对值均值 | `mean(abs(diff))` | — |
| 波动标准差 | `std(diff)` | ddof=1（样本标准差） |
| RMS | `sqrt(mean(diff^2))` | — |
| 峰峰值 | `max(diff) + abs(min(diff))` | **不是** max - min，UI 和导出中须注明口径 |
| 最大正向跳变 | `max(diff)` | — |
| 最大负向跳变 | `min(diff)` | — |

### 2.5 轨迹 ID 命名

- 格式：`{原始ID}_seg{序号}`，如 `64_seg2`
- 序号从 1 开始，按时间顺序在同一原始 ID 内递增
- 序号不可复用、不可跳号

---

## 三、数据处理规则

### 3.1 时间戳解析

CSV 中时间戳格式为 `YYYY_MM_DD_HH_MM_SS_mmm`（下划线分隔）。用户输入格式为 `YYYY-MM-DD HH:MM:SS`。统一规则：

1. `data_loader.py` 提供唯一的 `parse_timestamp(ts_str) -> pd.Timestamp` 函数
2. 支持的输入格式（按尝试顺序）：
   - `YYYY_MM_DD_HH_MM_SS_mmm`（CSV 原始格式）
   - `YYYY-MM-DD HH:MM:SS`（用户输入）
   - `YYYY-MM-DD HH:MM:SS.%f`（含毫秒）
3. 解析失败抛出 `ValueError`，由调用方决定提示信息
4. **禁止在多个地方各写一套时间戳解析逻辑**

### 3.2 预处理顺序

```
加载 CSV → 剔除空值/NaN → Track_Age 类型校验(0~255) → 时间戳解析 → 全局按时间排序 → 按 ID 分组
```

- 物理量超量程的行在预处理阶段剔除，不在分段阶段处理
- 排序必须全局排序，不能先分组再排序（保证跨 ID 的时间顺序正确）

### 3.3 数据缓存

- 分段结果在服务端缓存（`flask-caching`），key 为 `{文件路径}_{文件修改时间}`
- 切换雷达位置时清空所有缓存
- **禁止缓存未分段的原始 DataFrame**（内存占用过大），只缓存分段后的元信息表和按段索引的数据

---

## 四、Dash 回调规则

### 4.1 回调链顺序

```
[1] on_radar_change     → 加载CSV → 分段 → 缓存
[2] on_timestamp_input  → 筛选时间窗口 → 提取ID列表
[3] on_id_select        → 筛选该ID轨迹段 → 更新表格
[4] on_trajectory_select → 加载轨迹数据 → 绘制曲线 + 全段统计
[5] on_quantity_change   → 切换Y轴 → 重算波动统计
[6] on_box_select        → 区间过滤 → 局部统计 + 高亮
[7] on_export            → 读取当前状态 → 生成文件
```

### 4.2 状态管理铁律

1. **上级变更必须清空下级所有状态。** 切换雷达 → 清空时间戳/ID/轨迹/曲线/框选。切换时间戳 → 清空 ID/轨迹/曲线/框选。以此类推
2. **客户端状态用 `dcc.Store` 存储**，包括：当前雷达位置、当前时间戳、选中的 ID、选中的 trajectory_id、选中的物理量
3. **服务端大数据用 `flask-caching` 存储**，包括：分段元信息表、按段索引的 DataFrame
4. **禁止在回调之间通过全局变量传递数据**——Dash 多worker 环境下全局变量不共享

### 4.3 框选交互规则

- 绘图用降采样数据（`plotly-resampler`），统计用原始全量数据
- `selectedData` 返回的是 x 轴范围，用该范围去全量 DataFrame 做 mask
- `selectedData === null` 时恢复全段统计
- 框选区间 < 2 帧时返回提示，不报错

---

## 五、性能规则

| 场景 | 阈值 | 策略 |
|------|------|------|
| 单轨迹渲染 | > 2000 帧 | `plotly-resampler` 自动降采样，统计用全量 |
| 全量数据 | > 50000 行 | 预构建时间索引（按分钟分桶 `pd.cut`） |
| 单 CSV 文件 | > 200MB | 分块加载（`pd.read_csv(chunksize=...)`） |
| ID 列表生成 | > 100 个 ID | 下拉框启用搜索过滤 |

**禁止事项：**
- 禁止在回调中遍历 DataFrame 逐行处理，必须用向量化操作
- 禁止在每次框选时重新执行分段——分段只做一次，框选只在已有段内过滤

---

## 六、异常处理规则

### 6.1 用户可见错误

所有异常必须转化为用户可理解的中文提示，**禁止将 Python traceback 直接展示在界面上**：

| 场景 | 提示文案 |
|------|----------|
| CSV 缺少必要字段 | 「数据文件缺少必要字段 {字段名}，请检查文件格式」 |
| 时间戳无法解析 | 「时间格式不正确，请使用 YYYY-MM-DD HH:MM:SS 格式」 |
| 时间戳超出范围 | 「输入时刻 {ts} 超出数据范围（{最早} ~ {最晚}）」 |
| 窗口内无目标 | 「该时刻 ±{N} 秒内无有效目标」 |
| 框选样本不足 | 「选中区域数据过少，无法计算波动」 |
| 雷达数据文件不存在 | 「未找到 {前角/后角} 雷达数据文件，请检查配置路径」 |

### 6.2 数据层异常

- Track_Age 超出 0~255 → 标记异常行，不参与分段，记录到日志
- Track_Age 非整数 → 同上
- 展开后 Track_Age 非单调 → 标记异常段，`is_abnormal = True`，仍参与分段但单独列出
- **禁止因单行异常而中断整个数据处理流程**——跳过异常行，继续处理

---

## 七、编码规范

### 7.1 命名

| 类型 | 规则 | 示例 |
|------|------|------|
| 函数 | snake_case，动词开头 | `segment_trajectories()`, `calc_wave_stats()` |
| 类 | PascalCase | `RadarDataLoader` |
| 变量 | snake_case | `track_age`, `gap_threshold` |
| 常量 | UPPER_SNAKE | `GAP_THRESHOLD`, `WRAP_HIGH` |
| trajectory_id | `{原始ID}_seg{序号}` | `64_seg2` |

### 7.2 类型标注

- 所有 `core/` 模块的公开函数必须写类型标注（参数 + 返回值）
- Dash 回调函数可不写返回值标注（Dash 装饰器已约束）

```python
# 正确
def segment_trajectories(df: pd.DataFrame) -> tuple[pd.DataFrame, pd.DataFrame]:

# 错误
def segment_trajectories(df):
```

### 7.3 注释

- 分段算法的每个规则分支必须有注释标明对应规则编号（A/B/C/D）
- 配置参数必须有注释说明物理含义和取值范围
- **禁止写"这行代码做了什么"的废话注释**，只写"为什么这么做"

### 7.4 引号

- 代码中字符串定界符统一使用单引号 `'`，需要内嵌单引号时用双引号 `"`
- JSON/YAML 配置文件按各自格式标准（双引号）

---

## 八、测试规则

### 8.1 必须覆盖的测试用例

| 模块 | 测试用例 | 验证数据 |
|------|----------|----------|
| segmenter | ID 首帧 Track_Age = 14，应正常识别为段起点 | case19 ID=55 |
| segmenter | 255→0 回绕，应延续不切分 | case19 ID=55 |
| segmenter | 83→14 非回绕下降，应切分（ID 复用） | 路试数据 ID=64 |
| segmenter | 时间间隔超阈值，无条件切分 | 路试数据 ID=33 |
| segmenter | 重复帧（diff=0），不触发切分 | case19 |
| segmenter | 单 ID 无断点 → 1 段 | case19 |
| segmenter | ID=64 → 3 段（3 个不同物理目标） | 路试数据 |
| wave_calc | 首帧无波动值 | — |
| wave_calc | 段内间隔 > 2x 采样周期处跳过差分 | — |
| wave_calc | 峰峰值 = max + abs(min)，不是 max - min | — |
| data_loader | CSV 格式时间戳解析正确 | — |
| data_loader | 用户输入格式时间戳解析正确 | — |

### 8.2 测试数据路径

- case19: `E:\feishuDownLoad\390s\kpi\case19\CD701_flr_track_2026_04_20_11_12_27.csv`
- 路试多 ID: `E:\feishuDownLoad\390s\行车路试\20260623-巡库-5.0.001b.0015.AR21\CD701_flr_track_2026_06_23_10_31_42.csv`

### 8.3 测试规则

- `core/` 模块的测试不依赖 Dash，纯 pytest
- 测试中禁止硬编码分段结果数量——用实际验证过的数据对比
- 回调测试可选，但分段和波动计算的测试必须覆盖上述用例

---

## 九、配置参数

所有参数在 `config.yaml` 中定义，`config.py` 提供默认值兜底：

| 参数 | 默认值 | 说明 | 可调场景 |
|------|--------|------|----------|
| SAMPLING_PERIOD_MS | 50 | 标称采样周期（ms） | 不同雷达型号 |
| GAP_THRESHOLD | 500 | 时间间隔切分阈值（ms） | 不同场景 |
| MIN_TRAJ_FRAMES | 20 | 有效轨迹最小帧数 | 分析精度需求 |
| WRAP_HIGH | 250 | 回绕判定上限 | Track_Age 位宽变化 |
| WRAP_LOW | 5 | 回绕判定下限 | 同上 |
| TIMESTAMP_WINDOW_SEC | 30 | 精确定位窗口半径（秒） | 分析习惯 |
| DEFAULT_QUANTITY | Dx | 默认展示物理量 | — |
| MAX_OVERLAY_CURVES | 4 | 最大叠加曲线数 | — |

**禁止在代码中硬编码上述任何参数值。** 必须从 `config.py` 引用。

---

## 十、导出规则

1. 所有导出文件名包含雷达位置标识：`{radar_position}_{trajectory_id}_{quantity}_{timestamp}.csv`
2. CSV 导出使用 UTF-8 with BOM 编码（Excel 兼容）
3. 图片导出使用 Plotly + kaleido，格式 PNG（默认）或 SVG（可选）
4. 图片标题包含：雷达位置、轨迹 ID、物理量名称、统计区间（全段/选中区间）
5. **导出选中区间时，必须导出原始全量数据中对应区间的行，不是降采样后的数据**

---

## 十一、版本与变更管理

- 需求文档当前版本：V2.1
- 任何对分段规则（2.1 节）或波动计算口径（2.3-2.4 节）的修改，必须同步更新需求文档并记录修订原因
- 新增功能不影响已有算法逻辑时，只需更新对应章节
- **Track_Age 四层分段规则（A/B/C/D）已通过两组实测数据验证，视为稳定基线，不可随意修改**
